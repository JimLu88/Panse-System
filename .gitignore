# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.sqlite3
*.db

# Node
node_modules/
dist/
.vite/
*.log

# Env
.env
.env.local
*.local

# IDE
.vscode/
.idea/
.DS_Store

# Docker volumes / data
data/
pg_data/

# Alembic generated cache only — keep versions
backend/alembic/__pycache__/
*.tsbuildinfo
.claude/

# 部署时由看门狗写入的版本标记 (每次 build 前重新生成)
backend/build_version.json
audit_xlsx.py

# 运行时数据 / 备份 / 上传 / 打包产物 — 绝不入库 (含财务数据与隐私截图)
backups/
storage/
deploy/windows/build/
# dist/ 已在上方 Node 段覆盖 (含 deploy/windows/dist/)
# 本地 compose 覆盖 (文件自注「请勿提交」, 仅本机端口/飞书开关)
docker-compose.override.yml

# 本地存档(SKU/工厂源图, 不入库)
_archive/

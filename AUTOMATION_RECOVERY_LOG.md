# 自动化故障与恢复经验日志

本文件采用追加式记录，保存自动化中已经确认的高成本故障原因、修复方式和验收证据。日常每一次失败事件仍由 ERP 的“失败事件记录器”按日期完整记录；本文件只沉淀可复用的工程经验，不保存账号、口令、二维码、金额或其他敏感数据。

## 固定记录规则

1. 订单拉取与推送、余额与收支拉取、活动自动报名的每次失败，都必须先写入 `scheduled_job_runs`，再由“失败事件记录器”按日期归类展示。
2. 重试成功不能删除原失败记录，只能把原事件标为“已恢复”，并保留失败原因、尝试次数、恢复时间和最终证据。
3. 订单任务分别核对：平台新报表、ERP 导入标记、下单图生成、飞书送达；不得用“任务已触发”代替成功。
4. 财务任务分别核对每个账户的新鲜度和落库标记；飞书只在登录失效或失败时提醒，不显示金额。
5. 活动报名只允许程序执行；AI 只能诊断和报告错误，不能自行提交、改价、扩范围或重试。
6. 每次修复后必须记录代码提交、生产版本、测试和真实运行结果；没有真实证据时写“未验证”，不得写“已修复”。

## 2026-08-07：订单下载与财务账单恢复

### 已确认原因

- 淘宝订单导出：平台已经生成新报表，但旧附件链接不会稳定触发 Playwright 的 `download` 事件，导致程序误报下载超时。
- 微信聚合账单：旧流程仍点击历史菜单位置，实际页面已改版；同时当月账单按平台规则尚未生成时，程序把“无可下载账期”误判成失败。
- 万师傅：独立账号登录态失效，旧流程最后只表现为“找不到导出按钮”，没有把真正的登录失效原因暴露出来。
- 发布过程：GitHub TLS 偶发中断可能让 API 已更新而 Web 仍停留在旧提交，因此必须以 API/Web 同一提交的正式验收为完成标准。

### 修复

- 淘宝附件改为使用同一浏览器登录上下文对“本次新生成的精确报表行”做认证请求，校验 HTTP 状态、文件名和 XLSX ZIP 文件头，并在截止时间内做短暂重试。
- 微信聚合账单改为确定性导航新版账单页，关闭引导层，分别检查收入/支出；账期尚未生成时返回 `no_data=true`，作为正常完成而不是失败。
- ERP 接收层允许财务任务以 `no_data=true` 完成，并照常写当日成功标记；没有 `no_data` 且没有文件时仍然失败。
- 万师傅通过已登录淘宝完成官方 OAuth 授权，后续真实财务任务仍须以成功导出/落库验证登录态。
- 发布验收必须通过 API、Web、数据库迁移和容器健康四项检查。

### 验证证据

- Web-Agent 回归测试：订单导出相关 `18 passed`；本轮已有修复累计提交均已推送。
- ERP 关键自动化与失败记录器测试：`17 passed`。
- 淘宝真实附件：认证请求返回 HTTP 200、XLSX 文件头有效。
- 微信聚合账单真实页面：返回 `ok=true, no_data=true`，原因是最新账期尚未生成。
- 生产发布：API 与 Web 同为提交 `7e708af`，数据库迁移 `0136 (head)`，API/Web/DB/backup 四容器运行。
- 当晚订单真实运行：平台生成并下载三份本轮新报表；订单报表、宝贝报表成功入库，发货报表进入待口令状态，因此没有越过安全门生成或推送下单图。
- 当晚财务真实运行：推广流水、万师傅账单和三项余额快照生成新产物并成功入库；聚合月账单返回正常“无新增账期”；主力支付宝余额与流水明确为登录失效，未伪报成功。
- 资源收口：本轮结束后已停止 8500 Web-Agent 服务，只保留按需唤醒桥。

### 第二层根因与追加修复

- 淘宝发货报表不是普通 XLSX ZIP，而是密码保护的 OLE/CFBF 容器。旧文件头校验把平台真实加密报表误判成无效文件；现仅对“发货报表”接受该容器，其他报表仍必须是正常 XLSX。
- “发送密码”不是页面弹窗，而是报表行 `data-secretquery` 对应的独立接口。程序现记录 `sent / no_delivery_channel / too_frequent / platform_rejected` 等非敏感结果，不再只盲点按钮后假定已发送。
- 本轮平台返回“未绑定接收渠道且请求过于频繁”，新发货表口令没有送达；已有口令不能解开新文件。系统保持待口令，收到匹配口令后走既有幂等续跑：解密入库 → 新鲜度关门 → 下单图生成 → 飞书送达。
- 对应 Web-Agent 提交：`b64c65e`（接受加密发货表）、`1156106`（记录密码发送真实状态）。

## 2026-08-07：发货口令成功后被异批次旧文件误拦

### 已确认原因

- 本次订单任务的精确产物清单为 3 份报表，口令回填后 3 份均已成功入库；但旧逻辑查询的是“当天所有仍处于待口令状态的淘宝文件”。
- 同一天另一批次遗留的 `ExportOrderList26924346852.xlsx` 仍为待口令状态，于是系统把当前已经完成的批次误判为 `shipping_password_still_pending`。
- 文件扫描时间、导入记录 ID 和文件名编号都不能可靠证明两份文件属于同一批次；用这些字段做全局新旧覆盖仍会误判。

### 修复

- 每次订单拉取持久化并读取该任务返回的精确报表清单；新鲜度门、口令回调、定时续跑、健康检查和订单编排均只核对这个清单。
- 当前清单中的每一份文件都必须以其最新持久化状态为 `imported`，缺失、待口令或失败时仍然阻止推送。
- 其他批次遗留的待口令文件继续保留用于审计，不删除、不伪装成功，但不再污染当前批次。
- 初始修复提交 `2b4be28` 采用了到达时间推断；生产数据证明该假设不可靠后，已由 `7bbdf2e` 的精确任务清单方案取代。

### 验证证据

- 回归测试：订单续跑、订单推送和失败事件记录器共 `51 passed`。
- 生产版本：API 为提交 `7bbdf2e`，健康检查返回正常。
- 真实批次：3 份任务报表状态均为 `imported`，本批次待口令数为 0；异批次旧待口令文件仍可在审计查询中看到。
- 真实续跑：生成 5 张新图，向飞书成功送达 14 张尚未送达的下单图，失败 0 张；剩余 1 张仅因平台收货地址仍隐藏而明确暂缓。
- 自动化事件：当天订单链路已标记为成功，之前 3 次失败记录保留并追加“已恢复”通知，不删除历史证据。

## 2026-08-07：订单人工恢复路径统一收口

### 已确认原因

- 发货口令回调和手动扫码续跑绕过了正常定时任务的收尾入口：可以完成解密和下单图推送，却不会同步工厂飞书表，也不会向失败事件记录器写入可关联的恢复成功记录。
- “三份订单报表”此前只是流程约定，不是代码硬门槛；少于三份时仍可能写入新鲜度标记，造成不完整批次被误判为可推送。
- 口令回调只处理最多 50 张待送达图片；晚于最后一次定时补跑才给口令时，大批增量图片可能被截断并留到次日。
- 失败通知曾把 18:30 写成“订单拉取重试”，但 18:30 实际只做下单图检查与推送；真正的拉取补跑是 19:17、20:17、21:17。
- 订单链即使已经成功，旧 `last_error` 仍可能保留，导致状态页继续展示过期口令错误。

### 修复

- 新增统一恢复收尾服务：口令回调和手动扫码共同执行“幂等补推下单图 → 单向同步工厂飞书表 → 写恢复阶段 → 关闭重试链 → 写失败记录器恢复证据”。任何一步失败都会保留精确原因并进入后续重试，不能误报成功。
- 当前批次必须恰好包含 3 个唯一报表文件，且逐文件最新状态均为 `imported`；少报、缺失、待口令或入库失败均不开放新鲜度门。
- 恢复入口单次处理上限由 50 提升到 500；已推送标记继续保证重复执行不会重复发图。
- 通知和失败记录器中的订单拉取重试时间统一为 19:17、20:17、21:17；18:30 不再冒充拉取重试。
- 成功状态统一清空旧 `last_error`，并由非调度回调写入 `scheduled_job_runs`，使当天原失败事件保留但状态改为“已恢复”。

### 验证证据

- 回归测试：订单取数、口令回调、下单图、失败记录器、告警和调度相关共 `84 passed`；追加状态清理回归 `29 passed`。
- 代码提交并推送：`ee1e1bb`（统一恢复收口）与 `b355bae`（清空恢复后的旧错误）。
- 生产发布：API 与 Web 同为 `b355bae`；健康、就绪、数据库迁移 `0136 (head)` 和四个生产容器均通过正式验收。
- 生产批次：当前精确清单为 3 份，逐文件状态全部 `imported`，本批次待口令 0。
- 生产收口：下单图重复推送 0 张，地址仍脱敏而明确暂缓 1 张；工厂飞书表 194 行同步成功，首次补齐更新 14 行，再次幂等核对更新 0 行。
- 失败记录器：当天 4 条订单失败历史全部保留，`open_count=0`；订单链 `success=true`、`last_error=null`、最后阶段为 `recovery_closeout/ok`。
- 资源状态：8500 Web-Agent 已离线；隐藏的按需唤醒桥保持运行，常驻看门狗继续禁用。

# 计划 7 单品立减小促价与仓库定制 SKU 一次性修正

## 已封口的前置事项

- 三个单品立减活动 `143780562424`、`143936811502`、`143939511827` 已改为
  `2026-09-01 00:00:00` 至 `2026-09-05 23:59:59`，旧改时入口永久退役。
- 商品 `1007407909979` 的 4 个审核 SKU 已补入活动 `143939511827` 并完成逐 SKU
  回读，旧补报入口永久退役。

## 活动 143780562424 的 20 SKU 小促价修正

- 固定商品范围：`1036273574687` 8 个 SKU、`1074244132390` 12 个 SKU。
- 只把当前 `mid_buyer_price` 对应立减金额改为 ERP `small_promo` 对应金额。
- 配件 SKU `6280268983408` / `PPS2633011022619` 永久排除，因为当前到手价
  `136.63` 低于小促目标 `140.00`，修改反而涨价。
- 定制、咨询、无 `small_promo` 行全部排除。禁止新建活动、增删商品、改报名价、
  改活动状态或发送通知。
- 写前重新核对三个活动、来源快照、ERP 20 行和平台 20 个旧金额；写入一次性 claim
  后，两商品各只允许一次精确“确认修改”。成功、失败和未知结果都不自动重试。
- 每个确认点击单独记录。若第一件成功、第二件失败或结果未知，回执会明确列出已回读
  商品和不确定商品，不会把部分成功伪装成整体失败后重跑。
- 最终还要独立只读审计 20 个目标金额。01 唯一命令：

```powershell
& 'D:\AI\畔色ERP系统\ERP程序\scripts\campaign_correct_plan7_small_promo_nas.ps1'
```

## 仓库商品 SKU 6060112621275 的 1500 → 1420 修正

- 固定商品 `1038725569412`，固定 SKU `6060112621275`，规格“尺寸微定制”，数量 6，
  SKU 状态“上架”，商品必须始终保持“仓库中”。
- 完整 14 行编辑器基线 SHA-256：
  `cf079f0b2903fecf0e7267ce9331c91b31d7ee12cce06aff20508c98296ecde5`。
- 只允许第 14 行价格从 `1500.00` 变为 `1420.00`；目标 SHA-256：
  `4ad0097702e2126ff3b56a6bbaaf9e5d62c8849b5e4a5985e28d7b49e0266987`。
- 写前和写后都按仓库列表 10 列逐列核对：标题、商品 ID、总库存 13006、累计销量 1、
  30 日销量 0、质量分、创建时间、结束时间和操作列不得变化；只有最低价允许
  `1500.00 → 1420.00`，状态必须“仓库中 → 仓库中”。
- 编辑器 14 行按 DOM 原顺序逐单元格核对。除目标价格外，其他 13 行以及目标行的规格、
  SKU ID、cspuId、数量、商家编码、SKU 状态必须零变化。
- 最终只允许精确“放入仓库”或“仓库中”控件；程序没有“立刻上架”点击路径。
  未知弹窗或缺少明确仓库选项时立即返回人工交互门，并永久禁止自动重试。
- 登录墙/扫码墙立即返回，不等待 180 秒，也不自动发送二维码。
- 01 唯一命令：

```powershell
& 'D:\AI\畔色ERP系统\ERP程序\scripts\campaign_correct_warehouse_product_sku_price_nas.ps1'
```

02 只交付、测试、推送和部署程序，不执行以上两个真实业务命令。

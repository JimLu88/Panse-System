# 已入库、原取数终态缺失时的限定子单续推

用户2026-09-19明确要求上线后续推今天未完成订单。此入口仅供操作者按当前授权使用，非日常全量取数替代，不更改既有自动化门。

`POST /api/imports/order-sheets/resume-scoped`，admin/operator认证。

输入业务日期、原批次ID、三份当天已成功导入归档ID、1至50条精确子单号、32位hex唯一request_id；dry_run默认true。先核验原档hash/角色/日期/销售明细归属，再对返回ready范围用同一参数dry_run=false执行一次。结果直接返回每条真实message_id及工厂号；同一请求再次调用仅读保存结果，running/unknown不重放。

只生成和发送选中的未送达子单；不重取报表、不重导、不全历史push、不主单删图重建、不作废历史汇总、不关闭原批次失败/覆盖原完成标记、不触发全局工厂表同步。已送达、发送中或未知的子单保留。独立逐行发送使用原渲染、地址、退款、远期、样块、补差规则及原飞书群；数据库原子认领防并发重复发送。飞书缺消息ID视为unknown而非成功。

仅允许把SKU、产品编码和规格都为空的主单ID占位行，用同日销售明细唯一子单和订单报表一致SKU补齐，数量亦按该销售明细还原；保留修正前后及来源hash。已有真实SKU或数量冲突不覆盖，留held明细。

本次原批次orders-20260919-fca9a7d3462846c8aa512a3648f35916；原档3025 orders、3027 sales_detail、3033 shipping。目标3316428051023004178、3316871605005137171、3316871605005146353。第一条原报表为PPS2438001051311、2件，数据库占位行SKU空、1件，须程序按证据补齐后才发。七条历史汇总问题不在范围。

程序验收不替代本次真实发送结果。01执行固定入口、回读消息回执；02仅维护和部署。

## 防止再次覆写

发货/订单报表没有SKU、规格或数量列时，子单upsert保留已有销售明细身份和数量，不用空值清除、不用默认1覆盖2件。显式提供的新SKU/数量仍按原导入逻辑处理。占位修复的产品编码沿用既有P→PPS规范。该保护不新增报名或订单前置规则，不改变退款/金额/发单授权。

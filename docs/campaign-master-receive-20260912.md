# 官方母版接收修复（2026-09-12）

本次是程序与文件接收维护，不是平台报名回执。

- 用户已下载的三个 UUID 文件均为61044字节，SHA256相同。文件被保存到项目证据目录，
  没有再次生成或下载，没有改动官方 XLSX 内部部件。
- 原浏览器超时任务保留。`campaign_pin_signup_master.py --received-file` 通过同一原生成任务、
  当前页面的活动身份、60商品输入范围及精确文件名接收用户文件，另存人工交接回执。
  已固定的母版再次接收只读返回，不能覆盖成另一个文件或重放旧任务。
- 固定母版是60商品575SKU，SHA256
  `be5ae804a19ccf987381ec0d2dab76e374ede9e780c1cc338ae19084574e4e83`。
  本机登记为 `outputs/campaign-continuous/master-replacement-20260911/master.json`。
- 实际超级立减模板将让利比例/补贴金额移到X/Y列，N为活动价、Q为商品短标题。
  新增精确表头布局，不用位置猜测，也不改变旧活动模板布局、价格或20%规则。
- 合并单元格索引替代反复扫描，保持原查值及重叠拒绝语义。真实文件559非活动中SKU
  内存生成/读回耗时约0.28秒，输出未保存、未上传，不是报名或价格审批。
- `resolve_master`验证本地文件后返回可下载文件状态，不把旧超时改写为自动成功。

验证：ERP活动相关测试260项通过，新增接收绑定、旧/新布局和索引等价测试。
Windows脚本需与Web-Agent下载接收补丁一并安装；不涉及NAS数据库或API/Web代码改变。
全流程实测、旧专用Edge向正式宿主接续、部分失败等实际分支仍需验收。

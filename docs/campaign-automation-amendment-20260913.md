# 用户补充：固定基线、延期轮换与自动执行验收

本文件是2026-09-13用户当前直接指令的固定补充，不改变原报名价格、窗口、两表顺序和成功不重传规则。

## 同日补充：大促优先暂停不是报名失败

用户已确认：官方明确另一大促生效中，且说明在精确结束时间前暂停日常超级立减，在该时间内属于正常状态。使用Web-Agent固定详情输出super_reduce_pause_policy识别，不停整轮、不自动恢复/改价/撤回/重报，不算新增成功。原因不明或结束后仍暂停只放最终复核，继续其余可报名项；不能以一个提示解释所有商品。下次既有定时检查按结束时间后的新鲜状态核对，不新增预检/轮询任务。具体程序合同见Web-Agent程序/docs/campaign-priority-pause-20260913.md。

- 4件5SKU的首次固定定制原价按campaign-five-user-established-baselines-20260913.json建立；已存在基线不覆盖，以后降低不重新计算原价。
- 升降桌793202812082：用户已授权系统轮换，但明确“这次先不换”。保留待办，状态authorized_deferred、execute_now=false；不得因该授权自动派发本次或定时轮换，待用户明确启用该待办再执行。现有2000原价、400底线不变。
- 定时报名必须打通。业务验收与历史录像质量分离：录像仍持续记录、缺帧如实列明，不能伪造完整录像；有完整可核验官方回执和价格/范围/一次性提交台账时，历史采帧缺口不单独阻挡业务ready。
- 业务验收仍要求原任务终态、无pending/owner/结果未知，真实单品立减及报名两阶段成功回执，逐商品成功证据、文件完整性、录制阶段覆盖与未变化的程序版本。修改无效果或无法自动处理的商品保留例外，不要求所有平台不合格商品都报名成功才能运行定时。
- 定时沿用原每72小时官方入口发现、ERP可售与淘宝上架交集、2至3秒就绪间隔和分段活动窗口；不恢复旧预检，不自动轮换，不猜未来活动/结束时间。业务证据丢失/被改写、价格保护失败、结果未知、登录验证码仍阻挡相关写入。

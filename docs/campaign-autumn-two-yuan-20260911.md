# 本场秋季家装节最终价容差

2026-09-11 用户新指令由执行任务 03 转交 02：“你再查看一下程序，两元以内的全部都是可以放行的”。这是当前授权，不引用旧超级88正向52行容差来扩大范围。

- 仅 `49557/49560/3538210379`，北京时间 `2026-09-16 20:00:00` 至 `2026-09-27 23:59:59`、官方12%、ERP大促目标。
- 逐普通SKU用**已成功单品立减的真实金额**计算最终价，`abs(最终价 - ERP大促目标) <= 2.00`（含正负2元）放行。超过2元、非有效金额或最终价非正仍拒绝。
- 这是本场目标价比较的明确例外，可比大促目标低最多2元；不是全局底价规则变更，不让所有行主动减2元，不修改ERP日常价或已成功优惠。
- 普通报名价仍等于ERPdaily；定制仍走固定基线20%保护，不能借本容差再降2元；实际优惠成功/同窗口/不重叠、成功与未知不重传均保留。

## 使用

03 用原正式 `campaign_generate_current_files.py` 参数重新生成本场未成功范围即可，不新增参数、平台下载、预检或临时旁路。生成器、`validated_body`、原子 `claim` 自动读取同一个字节锁定授权；结果和bundle保存 `final_price_tolerance`，复用行保留实际金额及差额。授权缺失/被篡改、旧包缺少当前规则指纹时提交前拒绝，必须重新本地生成；不能修改旧包后重传。

授权文件：`docs/receipts/campaign-autumn-two-yuan-authorization-20260911.json`；代码：`scripts/campaign_scoped_tolerance.py`。其他活动/窗口/比例/目标默认零容差，不继承此次例外。冻结合同母文件保持不变。

本次只修本地入口，不部署NAS、不操作浏览器，不表示平台报名完成。刚成功两品 `1037239277197`、`719436834260`（03登记批828960129）继续由共享状态库保护。

验证：`python -m unittest discover -s scripts/tests -p 'test_campaign*.py'`；保存的组装快照可用 `verify_campaign_scoped_tolerance.py --assembly <原组装JSON>` 在临时状态库只读复算，不创建生产claim或上传文件。

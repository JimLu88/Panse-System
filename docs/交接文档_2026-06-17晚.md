# 畔色孚格 ERP — 会话交接文档 (2026-06-17 晚)

> 给下一个会话：先读这份 + 记忆文件 `C:\Users\lzdwy\.claude\projects\C--Users-lzdwy\memory\project_erp-batch-2026-06-17.md`，
> 就能无缝接手。**当前 git: `0e1a98c`，工作树干净，NAS/PC/GitHub 三方同步。**

---

## 一、项目是什么

**畔色孚格 ERP (Panse-System)** —— 家具电商（淘宝为主）的进销存 + 财务 + 定制报价 ERP。
- **后端**：FastAPI (Python 3.11) + SQLAlchemy + Alembic，目录 `D:\Panse-System\backend`
- **前端**：React 18 + TypeScript + antd 5 + @tanstack/react-query + echarts，目录 `D:\Panse-System\frontend`
- **部署**：群晖 NAS (DS923+) 上 Docker 跑 postgres + api + nginx(静态前端)；大文件(图库/归档/导出)放 NAS 共享盘
- **配套**：`D:\Panse-Web-Agent` —— PC 上的浏览器自动化 agent (Playwright)，抓淘宝/万师傅/支付宝等数据，由 NAS 在 18:00 驱动取数，结果走 ingest API 进库

## 二、文件夹路径（关键）

| 用途 | 路径 |
|---|---|
| 主项目 (后端+前端) | `D:\Panse-System` |
| 后端 | `D:\Panse-System\backend`（app/api、app/services、app/models、tests、alembic） |
| 前端 | `D:\Panse-System\frontend`（src/pages、src/components、src/api） |
| 部署脚本/配置 | `D:\Panse-System\scripts\deploy_api_nas.sh`、`D:\Panse-System\deploy\` |
| PC 浏览器 agent (独立仓) | `D:\Panse-Web-Agent`（app/api、app/browser、app/engine、app/tasks、main.py） |
| 持久记忆 (跨会话) | `C:\Users\lzdwy\.claude\projects\C--Users-lzdwy\memory\`（`MEMORY.md` 是索引） |
| NAS 部署目录 | `/volume1/docker/panse/`（compose 文件 + storage/gallery/backups 共享盘） |

## 三、部署 / 验证套路（务必照做）

**全程用 Git Bash，不要用 PowerShell**（PowerShell 管道会损坏二进制镜像流）。

- **后端 (api)**：`cd /d/Panse-System; BUILD=1 bash scripts/deploy_api_nas.sh`
  （脚本：build → docker save|gzip|ssh load → compose up api → 重启 web → 健康检查）
- **前端 (web)**：
  ```bash
  cd /d/Panse-System
  docker build -f deploy/web.lan.Dockerfile -t panse-system-web:lan .
  ssh -i ~/.ssh/panse_nas -o BatchMode=yes -p 2222 15068803006@DS923plus "sudo /usr/local/bin/docker tag panse-system-web:lan panse-system-web:rollback"
  docker save panse-system-web:lan | gzip | ssh ... "gunzip | sudo /usr/local/bin/docker load"
  ssh ... "cd /volume1/docker/panse && sudo /usr/local/bin/docker compose -p panse-system -f docker-compose.lan.yml up -d --no-build --no-deps web"
  ```
  ⚠️ **web 铁律：必须带 `-f docker-compose.lan.yml` + `--no-deps`**。漏了会用默认 `docker-compose.yml`(prod版, web=nginx:alpine+web-build+映射443)，撞宿主机 nginx 的 443 → web 起不来。恢复：`docker rm -f panse-system-web-1` 后用 lan 文件重建。
- **NAS 访问**：`ssh -i ~/.ssh/panse_nas -o BatchMode=yes -p 2222 15068803006@DS923plus`
  容器内跑 python：`sudo /usr/local/bin/docker exec -i panse-system-api-1 python` 接 heredoc。
  health 检查：`curl -fsS http://192.168.31.21:8200/api/health`
- **本地测试**：host Python 缺 bcrypt 等完整依赖 → `pytest` 跑不全。**在 NAS 容器内跑**：`docker exec -i panse-system-api-1 python -m pytest tests/xxx.py -q`。host 上 openpyxl 有(可读/造 xlsx)。host Python 读路径要用 `D:/...` 不是 `/d/...`(后者只 Git Bash 认)。

## 四、环境约束（红线，必须遵守）

1. **三方同步**：改代码后 NAS + PC工作树 + GitHub **同一 commit**。完成即 `git push origin main`（直接提交不开 PR，**无 AI 署名**）。
2. **绝不 `git add -A`**：只 add 本次改的具体文件（backups/storage/build 等已 gitignore；仓库根有 2 个无关未跟踪 docs `图生爆炸图_落地方案.md`/`定制报价v2_理顺提速_落地方案.md`，**别带上**）。
3. **D 盘强制令**：所有安装/缓存/大文件只放 D 盘，C 盘禁写。
4. **GateGuard 钩子**：每个文件**首次** Edit/Write 前，会拦一次，要求报 4 项事实（① Grep 引用方 ② 受影响函数 ③ 读写字段 ④ 用户原话）。报完**重试同一操作**即过。每文件每会话拦一次。Bash 首条也会拦一次。
5. **飞书推送**：`feishu_push_chat_id` 群是用户自己的(非外部)，可直接发。

---

## 五、本会话已完成（commit 链 1f3e192 → 0e1a98c，更早的见 git log）

任务面板 #1–#37 基本做完。重点：
- **#30 利润推算**（问题1）：`order_cost_service.auto_cost_backfill` 全自动——BOM/定价表反推 + 查不到的按**实付×类目成本率**(类目<5单退全店, 剔离群单)兜底 + 缺成本写 `DataException(cost_missing_estimated)`；接入 scheduler 每日06:50。实测补425单+107率兜底。
- **P→PPS 编码归一**：orders 117单+order_details 803单 `^P[0-9]{11}$`→`PPS+11`（数据已改，非代码）。
- **#33 Excel全量导出大改**、**#19/#25 排行短名+去退款**、**#27 待办自动完成+登录状态**、**#37 预测SKU展开+导出**、**#8 大盘日期筛选**、**#20 健康度卡移待办页**、**万师傅页恢复**、**下单图配图回退** 等。
- 详细全在记忆文件 `project_erp-batch-2026-06-17.md`。

---

## 六、🔴 待处理 / 进行中（下一个会话优先做这些）

### 1) #30 缺成本异常要剔除「退款/关闭单 + 2026年以前的订单」（用户当前最急）
**问题**：`auto_cost_backfill` 生成的 105 条 `cost_missing_estimated` 异常里，混进了**退款单、关闭单、2026年以前的旧单**——这些不是真实在产销售，不该估算成本、更不该报异常。
**改哪**：`D:\Panse-System\backend\app\services\order_cost_service.py`
- 加 `_skip_cost_estimate(order)`：满足任一即跳过（不估算、不报异常）——
  `order_date < date(2026,1,1)` / `status in (cancelled/closed)` / refund_status 含「退款/退货/关闭」/ 全额退款(refund_amount ≥ paid×0.9)。
- `auto_cost_backfill` 循环里：skip 的订单 `recompute_and_save(db, o, ratios=None)`（只用真实BOM/定价，不走率兜底），不加入 est_nos。
- `_flag_cost_exceptions`：**清理已生成的**——open 的 cost 异常，若其订单 `_skip_cost_estimate()` 为真 → `ex.status="resolved"`（把那 105 条里的退款/旧单关掉）。
- 改完部署 api，在 NAS 容器跑一次 `auto_cost_backfill(db)` 清理 + 重估。补测试 `tests/test_order_cost_backfill.py`。

### 2) 补单记录导入报错 + Excel 入口适配（用户给了样例文件）
**样例**：`C:\Users\lzdwy\Desktop\补单记录.xlsx` —— 用户说以后导入的补单全是这种格式，现在直接导进去**报错**。
**要做**：① 用 openpyxl 读这个 xlsx 的真实列结构（host 能读：`python` 里 `openpyxl.load_workbook("C:/Users/lzdwy/Desktop/补单记录.xlsx")`，注意 host 用 `D:/`/`C:/` 路径）；② 找补单导入入口修复——后端 `backend/app/services/bill_import_service.py`(`import_refill_simple_xlsx` 等) + API `backend/app/api/finance.py` 的 `/refill-records/import-csv`，前端 `frontend/src/pages/RefillRecordsPage.tsx`；③ 让通用 Excel 导入入口(`/importer` 页 + `excel_importer.py`/`smart_import_service.py`)也能认这种表。先看清报错和真实列，再改容错。

### 3) #16 PC 端本地定时自跑（还没开始）
**目标**：`D:\Panse-Web-Agent` 现在是 NAS 在18:00 来驱动取数；加一个**本地调度**（APScheduler/定时），让 agent 自己也按点取数，NAS 连不上时也能跑。
**现状**：agent 目录 `app/{api,browser,engine,tasks,main.py}`，grep 没找到现成 scheduler。需读 `app/api`(NAS 调的 run 端点)+`app/tasks`(取数编排)+`app/main.py`，加本地定时调用同一套取数逻辑（注意幂等：结果走现有 ingest，file_hash 防重）。**改完要让用户重启 PC 上的 agent 进程**（独立仓，不走 NAS 部署）。

---

## 七、⚠️ 已知数据问题（非紧急，建议清理）

- **补差价/部分付款单错配整份SKU成本**：有订单实付 ¥452 却背着 ¥2046 的 SKU 成本（如岩板餐桌的差价补款单），拖累利润。`#30` 的成本率计算已剔除这类离群单(成本/实付>1.2)，但**这些订单本身的 theoretical_cost 仍是错的**，建议单独把"实付远低于产品正常售价"的补差价/部分付款单成本归 0 或按比例修正。（与第六节第1项相关：退款/补差价/部分付款单的成本口径要统一治理）
- **211 单完全无 product_code**：导入时商家编码没提取进库（如订单 `3308616553940011766` 已手动修过）。根因在 taobao 导入解析，值得查 `taobao_order_import.py` 为何丢编码。

## 八、关键技术上下文（速查）

- **成本口径**：`order_cost_service.compute` —— 已知SKU 用定价表 `factory_cost`+运费/安装/税/扣点=`accounting_cost`；定制单走 BOM 反推；再缺用「同款正常单均价」占位。`theoretical_cost` 写回 order，每日06:50 `auto_cost_backfill` 兜底。
- **利润**：`reports.py _business_month` —— `total_expense = cogs(=max(工厂账单,Σ理论/实际成本)) + 推广 + 售后 + 外包 + 平台费`。全店成本率实测 ~0.73(27%毛利)。
- **下单图**：`order_sheet_archive_service`(生成HTML+推飞书) + `factory_sheet.build`(配图：pricing_sku.image_url，缺则回退同产品任一有图SKU的淘宝CDN图)。手动补推按钮在「工具→资料存档库」。
- **P↔PPS 前缀漂移**：订单常用 `P+11位`，产品档案是 `PPS+11位`；`sales_analytics._internal_names`、`dashboard_monthly_service._iname` 都做了兼容；数据层已批量归一。
- **待办台账**：`ops_checklist_service` —— 静态 SOP + `_auto_done`(按数据自动判完成) + `platform_login_status`(读 `web_agent_state`/`web_agent_pending_scan`)。
- **测试**：改后端务必在 NAS 容器内 `pytest`；新功能补 `tests/test_*.py`。

---

**一句话给下一个会话**：当前最急是 **第六节第1项(#30异常剔除退款/旧单)** 和 **第2项(补单导入报错)**，都是后端改 + 一次 api 部署；#16 是独立 agent 仓单独搞。照第三节部署、守第四节红线即可。

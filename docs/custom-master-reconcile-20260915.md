# 家具定制 ERP 主档补齐（一次精确授权）

用户2026-09-15明确：1001358847694报名并按淘宝价格建立ERP；863525290377差价链接不报。
唯一业务执行者03。维护只交付程序，不并发执行数据库业务写入。

固定入口 `scripts/campaign_custom_master_reconcile.py` 复用5条现有物理SKU映射，不生成新产品编码或SKU。
源为现有完整商品导出、原工作表SHA和5条精确规格/金额；平台商家编码为空，不伪称匹配了平台编码。
价格是2000、5000、9000、13000、20000元，逐物理SKU对齐；ERP旧价/名称必须仍匹配已读取状态。
只补商品主档、在售状态/商品ID并改5条daily_price与sku名称；成本、其他价格档、促销、库存、原订单映射及所有固定原价来源不变。

执行者调用 `--apply --scope <product-scope.json> --output <新回执路径>`。
`--plan`仅验证源并输出准备结果；`--readback`只查同一产品和已提交审计。
串行化事务、同授权数据库锁、精确旧值比较、物理映射唯一性检查，以及同事务before审计保证整组成功或整组回滚。
重复命令只允许验证同一已完成状态，发现后续人工漂移拒绝覆盖。若连接超时，先`--readback`，不因超时重写。

此入口是本地已审计固定运维程序，经已有专用SSH/psql通道执行；没有新增开放API或修改NAS在线服务，不需要重建服务镜像。
不建立首次原价、不写报名claim、不调用浏览器。ERP读回成功仍不等于淘宝报名成功；之后由固定报名程序使用新ERP快照进行两场授权范围报名。
10项离线单元测试和真实本地源计划校验通过；数据库实写验收由03首个授权执行回执提供。

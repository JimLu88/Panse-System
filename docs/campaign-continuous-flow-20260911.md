# 2026-09-11 用户确认的连续 Web Agent 报名流程

## 当前交付状态：程序核心已实现，真实运输绑定未完成，尚未上线

本文件及同名 JSON 记录用户本轮明确修改的长期规则，不从早期局部改价授权推导。
新流程不能调用旧的历史价格刷新、动销排除、候选扫描或预检入口。
旧的精确批次、已保存回执、固定原价和 unknown 防重放记录全部保留；不改旧 JSON
指纹让现有批次失去可验证性。新规则按版本迁移，不能把旧失败文件当新文件直接重传。

## 固定业务顺序

1. 每72小时从用户指定入口识别真实链接；AI仅承担找链接、确认新活动。入口为
   https://myseller.taobao.com/home.htm/starb/tmc-next/sale/seller/homepage.htm 。
   活动身份以店铺、活动/阶段ID、精确窗口为准，不能按标题或猜出的URL判断新旧。
2. 商品范围取 ERP 可售商品与淘宝上架商品的交集，下架不报，不按历史动销排除。
   完整产品快照须与页面商品总数核对，不能把当前页或“已选20”当全量。
3. 单品立减和超级立减分别使用各自固定官方母版的新数据副本。超级立减母版明确
   报模板错误才替换；其他活动下载本场新的SKU维度、含参考数据、不智能填价模板。
4. 同一价格版本生成两表；普通SKU报名价为ERP日常价，单品立减后按本场力度落到M/B。
   定制用既定固定基线规则，不额外叠加普通单品立减。
5. 单品立减明确成功后读回实际时间窗口，再上传活动表。已证明相同的成功立减复用。
   普通操作以页面就绪为准，目标间隔2–3秒；平台批次约180秒等终态，不把睡3秒当成功。
6. 每批错误报告只取一次，程序统一分类、修复可修复项，持续补报改变后的失败商品全SKU。
   没有任意“两轮即停”的次数限制；相同修正没有新效果不能反复提交。
7. 最后汇总成功、需轮换批准、其他无法自动处理项。轮换须用户当前确认；登录、验证码、
   无法完成的上传下载及时请求协助。完成处理不等于全部报名成功。

## 金额及分类边界

- 普通SKU相对同一ERP最终目标价的绝对差不超过2元含等号可自动修正；不能每轮再让2元。
- 我方文件报名价错了，纠正回ERP日常价；若已是日常价而平台仍要求其他标价，则人工处理。
- 明确券后/标价限制无法在目标±2元内解决时，列轮换候选，不承诺轮换清除历史或保证成功。
- 定制固定底线=既定原价×20%，不随降价重算，跨轮换继承。精确底线有分以下小数时，
  可提交最低金额向上取到分；例如541.67×20%=108.334，最低提交108.34。
- 缺基线、缺价格或分类不明都不是“必须轮换”的证据。映射必须用全量官方导出和唯一编码
  证据解决，重名或多匹配不能猜。备用SKU仍是灰色下架，保留库存。

## 已落代码及验证边界

- `scripts/campaign_continuous_policy.py`：入口/身份、新旧活动、交集、72小时、分类、固定底线。
- `scripts/campaign_continuous_flow.py`：独立持久表、同店单写入、步骤缓存、写前unknown、
  两表顺序、时间读回、成功保护、修正后的失败范围重报、重复修正收口。
- `backend/app/services/campaign_automation_service.py`：新自动入口不再执行旧预检链；
  运输未就绪时如实返回blocked，不调用旧上传或清除旧计划状态。
- `campaign_discovery_service.run_periodic_discovery`：先保存尝试时间，再执行发现；
  即使已有旧小时cron覆盖注册默认值，72小时检查点仍阻止反复扫描。未修改生产定时数据库。
- Web-Agent发现首页改为用户给出的myseller地址；新增只读能力报告，当前明确ready=false。

## 必须完成后才能称自动报名已上线

1. 用户已于2026-09-11回复“可以”，同意专用Web-Agent Edge会话。真实Edge已成功打开，
   本地虚拟文件选择测试通过，用户已完成登录，官方入口店铺与活动身份已经只读核验。
   独立会话宿主为Web-Agent分支的app/browser/campaign_edge.py（回环8502，Bearer保护），
   不复制旧档案、不打开网络CDP、不操作旧浏览器；不代表淘宝实际上传已经验证。
   8500按需停机本身不是故障，不改为常驻服务。
2. 将真实ERP/商品导出、固定/新官方模板及现有受控生成器接到controller的transport。
   目前测试用FakeWebAgent只做离线模拟，没有注册为真实运输，不得删除ready=false冒充接通。
3. 接入原优惠精确原位改额、终态及时间读回、原始失败报告解析、人工完成后的明确回执恢复。
   core已支持单品立减失败分类、旧失败精确报告导入和不可重放的终态恢复；
   已知截止提前触发已接入调度服务。仍需将这些接口与真实价格修正、完整商品导出绑定。
4. 校验真实适用SKU完整性、既有优惠及价格生成断言，由既有业务执行任务作为唯一写入者，
   实测秋季之后的超级立减（官方未开放时记录未开放，不编造未来活动或回执）。
5. 相称集成测试、正式发布、版本读回和生产72小时任务配置验收。当前未NAS部署、未平台上传。

这是一份诚实的实现检查点，不是“整个自动系统已完成”的证明。

## 2026-09-11 维护实现进展（不代表业务测试通过）

- 官方报告按物理工作表读取，不信任错误的A1尺寸；商品首行里的SKU限制按本商品完整
  属性定位，合并券后价/最低标价限制，缺证据不猜。只读旧31失败商品报告得到283SKU、
  126条限制；不是新增报名结果。逐SKU归一化、全商品导出覆盖/唯一编码匹配已有测试。
- 新建凭证可显式绑定dedicated_edge_v1；上传前原子消耗一次。旧unknown凭证不能补绑
  到新通道重放。新规则通过continuous_rule_sha选择，未携带该字段的旧文件保持原规则。
- Edge客户端仅提交一次异步任务，后续只读确切job；官方批号、文件名、基线新记录绑定。
  campaign_edge_receipt把官方终态回填原Authority；报告下载失败不会抹掉已确认终态。
- 已完成206项ERP活动脚本测试、18项后台定时测试。测试数据不构成真实报名回执。
- Web Agent录屏改为当前标签页内部取帧，不等待网页字体稳定；真实已登录单品立减页
  99帧MP4验证通过。此前录屏0帧TimeoutError保留为失败证据，不冒称录制成功。
- 专用Edge真实表单测试job：1ac8c8aeedc3b1fe8b1e1e6e2e86dd92fc5367fa1083501e03d6148932d7f0f0；
  起止秒值正确、已选批量导入/SKU级；upload_count=0、submit_count=0。不是实际立减成功。
- 完整controller生产adapter、新建优惠的官方ID/实际窗口归集、全局修正执行凭证、
  超级立减固定母版路径绑定及正式部署仍未完成。保持ready=false，不要求03提前报名。
- 最新用户范围：02做程序并完成交付；03仅在程序整体就绪后做最终业务测试。

## 日常优惠时间口径已确认并接入本地程序

2026-09-11本轮只读固定官方超级立减页显示：长期立减10%，平台长期
活动有效期到2028-07-31；这不是已经设置好的日常中促单品立减窗口。
如果机械地把该长期有效期当成单品立减时间，会与秋季/88VIP等大促优惠重叠。
用户随后明确答复“是的，就是分段设置时间”。独立新版本合同将长期活动有效期与
日常空档M/大促窗口B分开；本地生成、双阶段claim和连续controller已接入，
已报名日常商品只补新优惠段，不再次报名。24项新增测试，活动脚本共230项通过。
具体输入和验证边界见 `campaign-segmented-time-20260911.md`，旧合同/批次指纹不修改。
无需再问时间口径。上文完整adapter/正式发布/业务验收缺口仍未完成，不宣称全系统落地。

## 2026-09-11 导出与读回接线续作

- Web-Agent已将全页商品导出、精确优惠/SKU读回接到持久任务。ERP新增对应文件覆盖与
  金额/时间验证器；导出状态变化不再误判为记录身份变化，失败页和已下载文件分别保留。
- 页面控件真实问题已经定位：初始空壳、重复的SKU级文本、固定列遮盖主表按钮、
  SKU编号与金额框不在同一水平线。改为有限等待、真实单选控件、原生可点击按钮、
  data-row-key精确映射。没有force点击、修改DOM、刷新重传或替用户保存。
- 本地241项ERP活动脚本和33项Web-Agent相关测试通过。真实现有编辑框读回两条金额
  1836.79/1724.68；不是完整读回任务成功，更不是报名完成。旧失败job状态不改写。
- 录屏停止或失败报告下载异常，不抹掉已取得的平台终态；单品立减先保存官方结果。
- 新代码只在独立分支。专用Edge空闲模块热加载不重开浏览器；并非主程序/NAS发布。
- 尚需：完整生产controller adapter、真实创建优惠的ID及逐项回执接线、受控修正执行、
  超级立减固定模板可用路径、全链路录屏、正式发布及03最终业务测试。保持ready=false。

# 工厂发货备注分源合同（2026-09-09）

## 当前授权

用户确认：畔色379单（3307929459972009052）2026年10月发货；畔色375单（3316168239087075694）2026年12月发货。这是人工确认，不声称平台含有这些月份。不取消已有生产、不更换工厂号、不创建远期身份。月份不等于具体截止日；月初/月末经过后仍等确认。

## 正常同步

- 平台备注标签独立保存原文、导入文件SHA256及更新时间。新增/变化更新；缺列保留；同主单所有标签行显式为空才能清该平台字段。多行非空标签合并，冲突报告保留。人工备注和确认月份不被平台导入覆盖。
- 主订单买家留言映射买家留言。平台标签只影响发货安排，不含开工授权。人工月份保留生产还要求独立确认标志、原工厂身份、无显式生产停止/远期冲突。
- 工厂表 tblqn9PDPO0S69wZ 使用原有字段；备注含平台原文与人工月份，提醒说明具体日待确认，发货安排沿用已有选项。无月粒度日期伪造，不新增表结构/定时任务。已发货、已签收、取消、售后优先，历史备注留存而不产生当前发货提示。
- 缺图不清图、未知成本不清成本、人工列不覆盖，沿用既有白名单差异及写后回读。6小时同步周期不变。

## 发布后01唯一业务写入

1. `python -m app.cli.factory_shipping_confirmations --apply-two-confirmed-months`：先检查两单父子/工厂/生命周期，后单事务补人工事实；仅当前标签未采集时，从已证实归档2876按固定指纹读取两单标签，不全量导入、不发消息。重复执行无变化，但失败不得盲重试。
2. `python -m app.cli.factory_dispatch_audit --apply-table tblqn9PDPO0S69wZ`：一次正常字段同步。
3. `python -m app.cli.factory_shipping_confirmations`：只读两单精确回验。
4. `python -m app.cli.factory_dispatch_audit`：只读全字段差异，证明无残余更新。

0153仅加可兼容列，不破坏旧记录。回滚代码允许保留新增列；禁止降级删除人工确认。不能把代码部署或本地测试称为两单已更新，须保留01实际回执。

# 用户本次建立的固定定制基线：78 + 4

用户本轮明确允许31件中78条定制SKU以当前ERP价格建立**从本次起固定**的基线。不是找回历史首次价，不扩为全店重设许可。四条此前已明确建立的基线原样保留。基线沿已验证ERP业务编码/物理SKU关系继承，之后降低当前价不能重算原价或20%底线。

## 权威来源

- 本次有效78条：`receipts/campaign-78-custom-user-established-baseline-20260911-v2.json`，SHA256 `2a1c418d6d76ac97927b0997d6fb0bd825648d9c2502e798446a5e38727eea5d`。
- 此前有效4条：`receipts/campaign-four-custom-user-established-baseline-20260911.json`，SHA256 `bc350504d38f88e679585fe0c73f228f778f46e6ce04f5a06294d520062c296c`。
- 78条初版 `receipts/campaign-78-custom-user-established-baseline-20260911.json`（SHA256 `07f128c7b2567d9309f21f3d9346272b049193bd21fcd9f217ab8cbe91126d20`）**仅留审计，不得继续用作有效基线**。初版2条数学floor错误，不能声称它通过了78条验证。

`campaign-entry-sources.json`默认载入有效78条v2和此前4条；共享SQLite的`fixed_source_corrections`记录精确旧源→新源、更正原因、两条差异与时间。旧文件和sources旧登记不删除；读取时执行已审计替代并按路径/版本去重，错误旧源不再产生row_errors/uncertain。默认来源与重复SQLite登记不会加载两遍。同路径冲突指纹、审计文件被改或更正链循环必须拒绝。

本次精度差异只有商品840301943626的SKU `6041970032668`、`6242873591416`。原价均541.67，精确20%为**108.334**。因此可提交到分的最低价向上取分为**108.34**，108.33必须拒绝。不能把数学floor写成108.33或108.34；其他76条及此前4条不改。原价、授权、身份、行序/范围都没有变化。

## 正式更正机制与防复发

`campaign_correct_fixed_floor_source.py --old <旧绝对路径> --old-sha256 <旧hash> --new <新绝对路径> --new-sha256 <新hash>`仅接受原登记错误数学floor的精度更正；全部原价/身份/其他行内容和授权元数据保持一致。新floor必须精确等于original×0.20，不允许修改原本有效的基线、改价或扩大范围。事务保留旧源审计、登记新源和替代边；重复同一修正幂等，冲突拒绝，不触碰报名claim状态。

新增fixed来源在`register_source`阶段就验证精确20%计算，错误floor不再先登记后污染所有读取。它是本地数据完整性检查，不是新平台预检/批准门。

02已执行本次精确替代。旧坏来源指纹变为有效v2是必要修复；此前据坏来源生成但未提交的包不得手改，应从现有模板/快照重新本地生成。成功/未知批次仍按原状态保护，不由来源更正解锁或重传。之后新任务自动读取，无需用户再逐次授权这78条基线，也不要重新询问已确认的批号831931699。

## 验证

152项活动隔离测试通过。`python scripts/verify_campaign_user_fixed_baselines.py`在新进程读取真实登记，验证78条与已保存ERP精确78条回读一致、此前4条不变、82条按分最低价允许/再低0.01拒绝、降低当前价不重设基线。该脚本不查询新ERP价、不调用平台、不改报名claim；已验证替换别名继承使用合成测试，不代表创建了任何真实SKU。

当前这是基线/入口固化，不是78条已改价或报名成功。定制本场具体申报价仍由已授权执行者按当前官方失败结果处理，普通±2及原位立减修正边界不变。本次无需NAS部署。

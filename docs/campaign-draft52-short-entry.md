# 计划8：本次52普通SKU续报入口

业务所有者是01。该入口仅生成文件，不调用浏览器或报名服务，不上传单品立减，不修改ERP或淘宝价格。

## 本轮证据和授权

- 活动49462 / 49469 / signRecord3527841611；窗口2026-09-06 20:00:00至09-13 23:59:59。
- 已有立减144226431384：新官方导出53行，精确52普通SKU金额及窗口一致；额外805268708396/5647543899905不处理。导出的dimension错误为A1，读取真实sheetData行，不能依据dimension判为一格。
- 本场官方参考模板20260906020807：SKU维度、含参考数据、不填充、6草稿商品70行。
- 用户在01针对本轮14行到手价正向差0.10–0.90元答复“2元以内全部可以”。仅接受本轮52普通范围内正向≤2元差异，不接受低于目标、不改ERP日常价、不推广到未来规则。
- 18定制SKU原始保护基准未核清：保留在独立回执，不写入报名文件。24成功商品228SKU与本轮范围互斥，不重传。

## 使用

**本轮已于2026-09-06 02:22:11由01上传一次，入口已封口。下列生成命令仅供审计，不能再次执行；CLI在任何文件生成/ERP查询之前拒绝重放。**
已交付文件为 `draft52-20260906-verified/计划8-52普通SKU-仅活动报名.xlsx`，SHA256 `139ede27d6e9ad1862ee8ae2562a34dbf176495adc4d4159603a1fae1a7768e2`。
最新官方终态：批次795099195，0成功、6失败，要求全部SKU报名，缺失精确18定制SKU。52-only 已证实不能独立报名，不得重传。官方失败文件 SHA256 `dcd58fdafeac2151a5a286a733e6c5cbbee8b00e9252ca03763823fd613ad426`。这是本场失败后的处理事实，不新增常规平台预检；文件生成、公式检查通过不代表平台价格审核通过。

```powershell
python scripts/campaign_prepare_draft52.py --template "C:\Users\lzdwy\Desktop\「26年淘宝9月超级88超级88现货」商品导入模版20260906020807.xlsx" --discount-export "C:\Users\lzdwy\Desktop\单品立减_144226431384_202609060200.xlsx" --output-dir "D:\AI\畔色ERP系统\outputs\01a03341-b2cd-7810-92f3-66fad189521d\draft52-20260906-verified" --erp-snapshot "D:\AI\畔色ERP系统\outputs\01a03341-b2cd-7810-92f3-66fad189521d\draft52-erp-price-version-20260906.json"
```

若成品已由维护生成，01直接使用成品，不重复执行生成命令。目录存在时拒绝覆盖。

输入文件指纹和52固定范围来自 `campaign-draft52-continuation.json`。价格来自一次只读ERP事务，保存同版本指纹与逐行日常价、目标价、原立减及授权偏差；不做候选页扫描或价格证据刷新。

可传 `--erp-snapshot` 复用本轮已取得的同一ERP版本，不重复查库；不传时才执行一次只读查询。输出指纹记录实际使用版本。

生成状态 `file_ready_for_01_single_activity_upload` 仅代表本地文件可交给01，不代表已上传或平台已接受。01在精确活动的批量导入上传该文件一次，等官方终态约3分钟；失败下载一次错误报告，未知不重传。此次**不上传立减表**。

本场官方已证实要求整商品SKU完整。失败续办必须处理相应整商品范围；定制价格依当前授权及已有明确原价记录固定底价，不将此次降低后的价格递归作为基准。真实低于底价的冲突交01与用户处理，不凭缺基准自动轮换。

## 模板结构修复

只保留选定的SKU数据行，按原顺序紧凑排列；同时映射行号、单元格坐标和数据区合并范围。被删除的合并起始行含商品ID、官方折扣等信息时，把原锚点单元格原样带到新锚点，避免产生孤立商品片段。填活动价P，官方折扣S为空时补本场明确的“12%”，其余保留原折扣、库存和发货信息；禁止将“12%”擅改为格式不明的0.12。

ZIP部件集合不变，除SKU数据工作表外每个部件的内容逐字节不变；保留平台自定义属性、说明页、样式、关系。新旧工作表前3行原样保留。遇到未知公式、绘图、数据验证等行依赖结构在本地明确报错，不能悄悄丢失它们。

## 验证与交付边界

`python -m unittest discover -s scripts/tests -p test_campaign_draft52.py`

覆盖错误dimension、行选择与合并锚点、价格、隐藏模板部件、重复SKU、无效金额、正向2元上限、禁止低于目标、禁止定制混入等。
这是本地CLI的发布，不需要NAS API/Web部署或Web-Agent重启；不把无需部署说成生产报名成功。01继续独占浏览器。

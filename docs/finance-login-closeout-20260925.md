# 2026-09-25 财务登录接续修复

主力支付宝实际于23:30导入1份、错误0，余额也已成功。禁止重新导出已成功文件。

缺陷：start_pending_scans 在主力号成功后无条件关闭整条 flow_pull，遮蔽万师傅失败和聚合账单合同不匹配。删除此捷径，只保留 scheduler 全部来源持久证据检查；更新关系图，保护其他来源的失败。

万师傅程序在登录失效弹窗停下，未走底部淘宝第三方入口。本地实际验证：点击淘宝入口后到 oauth.taobao.com/authorize，页面说明淘宝已登录，但要求“授权并登录”。不能当扫码失效，也不能把授权页截图作为余额。

Web-Agent 增加同一持久浏览器内的有界SSO前置步骤，账单及余额共用。只点已核实的重新登录、淘宝入口；显式授权、扫码或验证挑战停下，结果保留具体原因；不执行LLM兜底、不导出、不放宽权限。

验证：ERP 67项相关测试通过。另有1项旧测试仍断言企业微信，而现程序早已改为飞书，本次未改该业务文案。Web-Agent登录状态8项测试通过。关系源已有其他指纹漂移，未全量冻结；新增两节点和保护边只代表代码关系，不代表全流水已恢复。

剩余：万师傅真实授权确认；聚合账单新版混合渠道CSV字段契约尚待适配，文件隔离未入账。产品表每款淘宝链接另有待办，不在本次财务修复中冒称完成。

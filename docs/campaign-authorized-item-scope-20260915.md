# 当前三商品授权的限定报名入口

用户明确报名家具定制1001358847694、九斗柜793052650673、升降桌793202812082；差价863525290377不报。
原 continuous_campaign_request_v1 新增可选 `authorized_item_scope`：

```json
{"authorization":"user-20260915-furniture-cabinet-lift","items":["1001358847694"]}
```

items允许这三个精确ID的非空不重复子集；家具可以独立完成。时间计划只允许原已确认两场窗口：秋季09-16 20:00—09-27 23:59:59、超级立减配套09-28 00:00—09-30 23:59:59。
其余身份、官方链接来源、模板、价格、安全及成功/未知防重放继续由原入口验证。

限制作用于持久scope动作的ERP候选集合，所有平台原件、SKU事实与ERP价格版本均保留不删行。
模板、两表导入、修复、最终逐SKU核对均使用同一实际限定集合。若目标不在ERP可售且淘宝上架集合，明确拒绝，不能把空范围说成全部成功。
不提供此参数的日常报名仍执行原全量规则；不是增加动销预检或任意项目范围接口。
没有启用通用SKU发布/轮换能力，没有清除既有成功或未知状态。ERP主档已补齐不等于平台报名成功。

执行03：据原真实两场页面身份及本场官方模板构造原generic请求，加入上述参数，复用此前固定导出job证明，但以新的ERP数据库快照生成。
一次启动后由原程序全段跑到终态；不能启动draft_only或do_not_register_or_execute文件。

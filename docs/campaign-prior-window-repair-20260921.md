# 长期活动报告接入修复（2026-09-21）

本场请求4200cd437ea3ab6b8c6adb74e7396e3b4a2f46c45a3bceb91c1743d9236d6da7处理结束但未新增提交。原报告读取器把原bundle的单品立减价格窗口直接与报名官方有效期比较；分段时间模式下这两个窗口本来就不同。现在调用既有phase_window(body, 'signup')，重新校验原绑定文件，按官方报名有效期校验终态。普通未分段请求行为不变。

同时兼容现行终态feedback.path/sha256/batch，必须完整且与原batch_id一致；旧两种引用同时存在时仍必须相同。原文件哈希、claim全量、SKU全集、提交价及已成功/未知保护不放宽。不改旧终态、价格、定时验收记录；不重启旧单品优惠。

原23件报告只读实证：15件可接入，8件原终态没有报告引用。15件中12件历史动销不足、1件历史报价不等日常价、2件仍需实际价格/优惠依据。此为历史报告分类，不是本次官方成功或本场新失败。

维护关系检查：此脚本在现有关系目录中仍为未详细登记项，不能因此认定无影响。直接影响报名错误分类、原请求失败恢复、最终状态汇总；没有订单、库存、财务、工厂表写入。测试覆盖现行/旧报告结构、分段/未分段、冲突哈希/窗口/claim/价格/范围及文件恢复；139项通过。

运行恢复交01通过原有recover-price-report一次接续同一请求。新实际旧优惠状态、映射缺口和最终导出核验仍须程序取得真实证据；不能凭本修复清空例外或宣布完成。

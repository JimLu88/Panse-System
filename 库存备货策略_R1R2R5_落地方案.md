# 库存 / 备货策略 落地方案（R1 + R2 + R5，R5 默认关闭）

> 2026-07-02 用户拍板。本文件是**续做锚点**：下次会话读它，从「六、落地步骤」接着做。
> 相关记忆：`project_panse-inventory-restock-method.md`。

## 一、背景 / 触发问题

用户在「成品库存」页发现三个问题，逐步暴露：
1. **推荐备货 200+ 张太高**（榉木餐桌 298）。→ 已修（见"已完成"）。
2. **成品库存不挂钩销售订单**：卖出不会自动减「现货」，它只是导入/手动/退货入库/售后拆件时变的静态快照。
3. **备货建议「需生产」只看缺口不看在产**：`需生产 = 预测30天 − 现成品库存`，**没减掉已经在工厂做/在途的量** → 重复建议、数字虚高。

## 二、已完成（本会话已三方部署，勿重做）

- **成品库存 ABC 分层 + 服务水平安全库存 + 批量（方向4+2）** — commit `c88d72e`。
  - 只算常规订单（`Order.is_custom==False`），定制不备成品。
  - ABC：A=累计80%畅销→自动备货；B/C=按需生产(MTO)，推荐0、状态`mto`不报警。
  - A类：安全库存=Z(服务水平95%)×日销标准差×√提前期；预警线=日均×提前期+安全库存；推荐=补到(预警线+批量30天)。
  - 参数在 `get/save_forecast_config`（system_settings）：`stock_service_level`/`stock_batch_cover_days`/`stock_abc_a_share`/`stock_abc_b_share`/`stock_min_daily`。
  - 实测：榉木餐桌 298→137、窄柜 83→40、0销量款→C·按需·0。
- **备货建议 常规/定制分开 + 定制通用料计划** — commit `a0d3a90`。
  - `_sales_by_day`/`forecast_30d` 加 `custom` 过滤（None/False常规/True定制）。
  - `stock_advice` 拆两段：常规单(成品产能缺口+全部物料) + 定制单(只倒推**通用料** `Material.is_custom==False`)。返回 `{products, materials, custom_products, custom_materials}`。
  - 前端 ForecastPage「备货建议」tab 加「定制单·通用料备货计划」卡。

## 三、调研结论（成熟供应链怎么做）

- **CODP 客户订单解耦点 + 混合推拉**：现实供应链都是混合，关键是"解耦点设在哪、在那囤什么"。家具典型多解耦点：标品解耦在成品 / 半标解耦在部件·半成品 / 全定制解耦在原料。
- **ATP 可承诺量**：`ATP/真实缺口 = 现货 + 在产在途 − 已锁定 − 欠单`。这就是修「只看缺口」的标准法。
- **Postponement / ATO 通用件延迟**：对多变体产品，囤"通用件/半成品(vanilla box)"而非成品，来单再组装/产成 → 降压货和呆滞。

## 四、用户决定（★核心，未来会忘，务必照此）

**全部做 R1 + R2 + R5 这套系统，但「半成品备货(R5)」默认关闭。**

- **R1（ATP 真实缺口）** — 做。`需生产 = max(预测 − (现货 + 在产在途 − 锁定), 0)`，扣掉在产/在途，不再重复建议、不虚高。
- **R2（多解耦点分层）** — 做。按 ABC + is_custom 分层：标品A→备成品 / 半标·微定制→备通用料 / 全定制→不备。（大部分已随 ABC + 定制通用料计划落地，需正式化标识。）
- **R5（半成品/白坯解耦）** — **代码全做，但默认关闭**：
  - 在「库存/备货页面 → 设置」里放一个开关（如 `enable_semi_finished`，默认 **false**）。
  - **关闭状态（现阶段）**：系统**统一按标品备货**（等于只跑 R1+R2），不出半成品计划、不要求产品打标。
  - **打开以后（未来量大 + 与工厂协商好再开）**：产品打标「可做白坯」、建半成品库存、出半成品备货计划，往这个方向引导设置。
  - **原因**：工厂备货要协商；现在囤很多白胚工厂会有意见、不一定同意。所以先建好能力、默认不启用。

**关联待确认（R3，用户本轮未点，续做时先问再动）**：
- R3 = 成品库存对 A 类做真实进出库（发货自动扣现货、生产入库加回），让「现货」变准。
- R1 的 ATP 里「现货」这一项的准确度依赖 R3；若不做 R3，现货仍是手动快照，ATP 仍可能偏。
- **续做时先跟用户确认**：要不要一起把 R3（仅A类成品发货扣减+生产入库）做了？还是现货继续手动、只先扣在产/在途。

## 五、R5 半成品设计细节（打开后怎么算，先记档，暂不实现主逻辑）

- **配货 = 不预分配，共享一个池子**：同一个白坯（如榉木餐桌1.4米白坯）既能做标品、也能做定制变体（改色/换岩板/微调尺寸）。谁的订单先到，就从池里取一个当场上漆/配岩板/装配（先到先得 FIFO）。到底给标品还是定制，是**订单来了那一刻才决定**。
- **备货量 = 归集需求**：`白坯备货量 = (该白坯所有成品：标品预测 + 各定制变体预测)全加 − 现有白坯 − 在产白坯 + 缓冲`。合并算 → 波动比分开囤小（池化效应），安全库存备得更少。
- **哪些产品适合做白坯（4条判据，满足越多越适合）**：① 一个前段能变多个成品(共用) ② 个性化步骤(漆/岩板/五金/微调尺寸)靠后且快 ③ 销量够且稳(A/B类) ④ 差异不在木架结构本身。系统可半自动挑：看某产品公共前段被多少 SKU/变体共用 + 差异是否集中在后段。
- **半成品不只木材，分三层解耦**：
  1. 通用料(原料级)：螺丝/双面胶/原片玻璃/岩板整料 —— 就是已建的「定制通用料计划」。
  2. **半成品(组件级)**：木作白坯 / 预切好的岩板玻璃 / 预装抽屉门板 / 五金包 —— **配件也能做半成品**(如预切常用尺寸岩板，比现切快)。
  3. 成品：上完漆配好装好(标品才囤)。

## 六、落地步骤（续做从这里开始）

### 阶段1 — R1（ATP 真实缺口）★最快见效，先做
- **后端 `sales_analytics.py`**：
  - 新增 `_in_production_by_product(db)` → `{product_code: 在产在途量}`。口径：`FactoryOrder.actual_delivery IS NULL AND voided_at IS NULL`，`sum(qty)` 按 `product_code` 归集（品牌变体用 `product_coder.brand_variants` 合并，与库存/ABC一致）。确认 FactoryOrder 字段名（qty / actual_delivery / voided_at 已在 `_compute_lead_time` 用过）。
  - `_bom_material_need(use_stock=True)` 里：`need_to_produce = max(forecast − in_stock − in_production, 0)`。products_out 增字段 `in_production`。
  - 定制段(use_stock=False)是否也减在产？定制成品不预备，通常不减；但定制"在产"消耗的通用料应算已在途——先按简单：定制段不减在产（保守）。续做时定。
- **前端 ForecastPage「备货建议」**：产能缺口表加「在产/在途」列；需生产列展示扣减后的值；导出加该列；说明补一句"已扣在产/在途"。
- **成品库存页(可选)**：`可用天数`/`推荐备货` 是否也纳入在产？成品库存的推荐备货是 A 类补货，可加"在产"扣减避免重复补。续做时定。
- 测试 + 三方部署 + live 对比（榉木餐桌：预测59 现货2 在产? → 需生产应=59−2−在产）。

### 阶段2 — R2（分层正式化）
- 大部分已随 ABC(成品库存) + 定制通用料计划(备货建议) 落地。补：在备货建议/成品库存加清晰的三层标识文案（标品·备成品 / 半标·备通用料 / 全定制·不备）。
- 如需自动判"半标 vs 纯标品"，可用是否有定制变体(改NN)/是否 is_custom 混合来分。续做时按需。

### 阶段3 — R5（半成品，代码全做但默认关闭）
- **设置开关**：`get/save_forecast_config` 加 `enable_semi_finished`（默认 false）。前端库存/备货页设置区加开关（关=灰显半成品相关，统一标品备货）。
- **数据模型**：
  - Product 加「可做白坯 / 半成品打标」字段(如 `semi_finished_eligible` bool + 可选 `semi_bom_stage`)；migration。
  - 半成品库存：**优先复用** `ProductInventory` 加 `item_type`(finished/semi) 字段，或新建 `SemiFinishedInventory`。续做时选（复用更省）。
- **服务**：`enable_semi_finished` 开时——备货建议多出「半成品备货计划」(归集需求：某白坯 = Σ用它的成品预测 − 现有白坯 − 在产白坯 + 缓冲)；共享池先到先得只体现在文案/口径，不需分配逻辑。
- **前端**：ForecastPage 加「半成品备货计划」卡(开关控制显隐)；产品页/库存页加打标 UI(开关控制)。
- **默认关闭时**：以上全部隐藏，行为 = R1+R2。
- 打开是"以后量大 + 工厂协商后"的事，本阶段只建能力、写好文档引导。

## 七、当前代码状态 / 部署
- main 最新（本会话）：ABC备货 `c88d72e`、定制通用料 `a0d3a90`（及之前飞书/定价一系列）。三方(NAS+PC+GitHub)同步。
- 部署方式：NAS 热补(base64|ssh docker exec) + restart + `docker commit :latest/:lan`；前端 build→dist→web容器→commit；git 三方同 commit（`git pull --rebase` 再 push，Jane 并发）。SSH：`ssh -i ~/.ssh/panse_nas -p 2222 15068803006@DS923plus`，docker 全路径 `sudo /usr/local/bin/docker`。
- 铁律：改测试勿回退代码；表格走系统导入口不手填DB；改完自己 live 测；D盘。

# 交接文档 — 大促报名 / 淘宝SKU映射 / 一码多SKU功能（2026-07-07）

> 给"新对话"看的完整交接。这段工作跨了很多轮，围绕**畔色淘宝店 88VIP大促报名**展开。
> 相关记忆：`project_panse-sku-taobao-remap`、`project_panse-pricing-reverse-gaijiaotai`（价格体系权威=仓库根 `价格体系设置.md`）。

---

## 0. 一句话现状
- ✅ **报名价表格已生成**：`桌面\大促活动报名_88VIP大促12%_最终报名价.xlsx`（579行，覆盖所有在架SKU，0漏）。用户导入千牛即可，但**导入前要先删掉"603坏价"那几个商品**（见待办C）。
- ⏳ **用户刚拍板的三件待办**（本文档核心）：A 开发"一码多SKU"系统功能；B 把156个定制/新变体SKU登记进系统；C 告诉用户"603坏价"商品的商品ID好让他从表里删。

---

## 1. 背景：这摊事怎么来的
1. **病根**：同事在淘宝把SKU尺寸"轮换"（如某skuId从1.8米改标1.2米）却没让商家编码跟着走 → 编码↔尺寸串位；然后 7-6 有人拿这份**错乱的淘宝表导入系统**，污染了 `PricingSkuPromo.taobao_sku_id` 映射。
2. **权威** = 7-3 DB备份（污染前）。已用它校验+修复。
3. **淘宝报名规则**：一个宝贝(商品ID)的**所有在架SKU都要报名**，缺一个整品失败，报错列出缺的SKUID。所以"缺SKU"= 系统里没有对上那个淘宝SKU。

---

## 2. NAS 直连 recipe（关键，改系统/取数都靠它）
```
ssh -i ~/.ssh/panse_nas -p 2222 -o StrictHostKeyChecking=accept-new 15068803006@DS923plus
docker 全路径：sudo /usr/local/bin/docker
api容器：panse-system-api-1   （docker exec -w /app panse-system-api-1 python3 ...，容器里有app+openpyxl）
db容器：panse-system-db-1（Postgres，端口不对外，只能容器内/exec）
storage：/volume1/docker/panse/storage  ↔ 容器 /app/storage
backups：/volume1/docker/panse/backups （每天03:00 pg_dump，panse-YYYYMMDD-030000.sql.gz）
```
- **scp的SFTP子系统被禁** → 传文件用 `ssh -T '…cat > /app目录/x' < 本地文件`；容器读文件把它放 storage 根(ASCII名)避开中文路径。
- **整库dump带客户PII禁下本地**；只在服务端抽非隐私列（pricing_sku/pricing_sku_promo等）再取。
- 生产库=NAS。改代码要**三方**（GitHub+NAS+PC同commit），但**PC docker当前是Web-Agent-only、down**，所以实际=GitHub提交 + NAS热补/烤镜像。

---

## 3. 已完成（别重做）
1. **修复系统映射**：把正确的 商家编码↔淘宝SKUID 通过 `excel_importer.commit_sheet(entity_type="pricing_sku", on_conflict="overwrite")` 导入生产（334行刷70 + 储物下柜8个），复核 will_change=0 全对；348 SKU导出就绪。
2. **交叉对照表**：`桌面\淘宝SKU对照系统_缺失标红.xlsx`（8列：淘宝SKU名/商品ID/淘宝SKUID/系统产品编码/产品名称/系统SKU名/SKU编码/状态；缺失标红）。588淘宝SKU：347已对应，241缺失（159系统无此编码 / 59淘宝未填编码 / 23一码多SKU）。
3. **用户在该表 I 列批注了241行**，5类（新对话必须按这个处理）：
   | I列批注 | 数量 | 处理 |
   |---|---|---|
   | 定制sku，按9折改价 | 156 | 活动价=现价×0.9 |
   | 不打折 | 53 | 活动价=现价 |
   | 一码多SKU（系统支持一对多，一起改）| 15 | 两个SKU都填、同报名价 |
   | 淘宝后台SKU未上架，不需改价 | 9 | 报名表剔除 |
   | 之前对应有问题/已补充编码，已修复 | 8+1 | 按系统报名价 |
4. **最终报名表**：`build_final_signup.py` 生成 `桌面\大促活动报名_88VIP大促12%_最终报名价.xlsx`。逻辑：13-21最新商品导出(588在架SKU) × 批注 × 系统报名价(reportprice)。结果579行 = 系统报名价370 + 定制9折156 + 不打折53 + 剔除未上架9。

---

## 4. 待办（用户已拍板，请继续）

### A. 开发"一码多SKU"系统功能（SKU级 一个商家编码→多个淘宝SKU）
- **现状**：`PricingSkuPromo.taobao_sku_id` 是单值(String64)。`Product.alt_taobao_ids`(JSON) 只是**商品级**，**SKU级没有**。
- **设计**（建议）：
  1. `app/models/pricing_ext.py` 的 `PricingSkuPromo` 加 `alt_taobao_sku_ids: Mapped[Optional[list]] = mapped_column(JSON, default=list)`（在 taobao_sku_id 后，约line 51）。
  2. **alembic 迁移**加该JSON列（backend/alembic/versions，照最新编号往后排）。
  3. **导出**两处都要"主SKU一行 + 每个alt再一行(同价)"：
     - `data_export_service.py::build_promo_signup_upload_xlsx`（约1206-1225，价格=`report_prices(p,params)[price_field]`）
     - `data_export_service.py::build_single_item_discount_upload_xlsx`（约1106-1155，优惠值=立减金额）
  4. **导入**：`excel_importer.py::_h_pricing_sku`(约1757) + `excel_schemas.py` 的 `pricing_sku` schema 增加 alt 列（逗号分隔淘宝SKUID）。
  5. **回填15个一码多码的alt**：从 `淘宝SKU对照系统_缺失标红.xlsx` 里筛 **状态含"一码多SKU"** 的23行 —— 每行给：SKU编码(G列) + 当前淘宝SKUID(C列) + 系统已对到的另一个skuId(H列状态文字里括号内)。让该sku_code同时挂两个skuId（一个主一个alt）。8个"已修复"的软包床(编码 PPS2633015011811~818)也是这种：软包床skuId(6069401344xxx) 与 悬浮床skuId(6069389752xxx) 共码，两个都要挂上。
  6. **三方部署** + 写单测（铁律：改成本/定价前看仓库 docs 口径；测试债清零传统）。

### B. 把 156 个定制/新变体 SKU 登记进系统
- 这156个是I列"定制9折"里、`系统产品编码`为空(❌系统里没有此SKU编码 或 淘宝未填编码)的。要在 `PricingSku`(+`PricingSkuPromo`映射) 建档，让以后系统导出自动带上。
- **数据来源**：`淘宝SKU对照系统_缺失标红.xlsx`（有淘宝SKU名/商品ID/淘宝SKUID/系统SKU编码col G[有的填了有的空]）。product_code 可由 sku_code 前缀推（去掉末2位）或从同商品其他SKU取。
- ⚠️ **需先跟用户确认**：定制SKU在系统里怎么定价？（占位/补差价性质，可能就是"标价×0.75当日常价、报名走9折"，或单独规则）。别擅自给成本。**登记前问清口径**。

### C. "603坏价"商品——告诉用户商品ID好删（用户要直接从表里删这些商品的全部行）
- **已确认坏的**：`中古岩板餐桌` **商品ID=1049596868352**，4个真实SKU(编码 PFG2621006010211~214) 系统报名价**全是603**（该产品成本/大促基数在系统里配错了，不是表的问题；对比同店蜂蜜餐桌2250-2505正常）。
- **疑似还有**：孚格 `PFG2621107` 前缀的产品也偏低。
- **动作**：用 reportprice.json 扫一遍，找出**所有真实SKU报名价 rp/现价 < ~0.35 或同款各尺寸rp雷同的**商品，把**商品ID列表**给用户（他从 `_最终报名价.xlsx` 里整品删掉）。或者用户去系统修好这些产品定价再重出（推荐修，但用户这次选删）。

---

## 5. 关键文件位置
- **桌面**（持久）：
  - `大促活动报名_88VIP大促12%_最终报名价.xlsx` ← 报名表成品（579行，删603商品后可导）
  - `淘宝SKU对照系统_缺失标红.xlsx` ← 交叉对照 + 用户I列批注（**唯一权威批注源**）
  - `0707 13-21-09 2690 / 13-21-09 7490 / 13-22-02 2210 / 13-21-08 8300 export.xlsx` ← 最新千牛商品导出（588在架SKU，18列：A商品ID/G商品级商家编码/J销售属性=SKU名/L skuId/M价格/P SKU级商家编码=sku_code）
- **旧会话scratchpad**（`C:\Users\lzdwy\AppData\Local\Temp\claude\C--Users-lzdwy\5f61c68a-a288-4c53-9f67-90e363f2eadb\scratchpad\`，可能失效）：`reportprice.json`(sku_code→rp)、`sysfull.json`(sku_code→product_code/名/映射)、`annotations.txt`、`build_final_signup.py`/`build_crossref.py`/`get_reportprice.py`/`get_sysfull.py` 等。**新会话若取不到JSON，就照下面重生成**。

### 重新生成 reportprice.json（新会话很可能要）
容器脚本要点（推到 /app/storage 跑）：
```python
from app.database import SessionLocal
from app.models.pricing import PricingSku
from app.models.pricing_ext import PricingSkuPromo
from app.services import pricing_calc_service
from sqlalchemy import select
db=SessionLocal(); params=pricing_calc_service.get_promo_params(db)
promo={p.sku_code:p for p in db.execute(select(PricingSkuPromo)).scalars().all()}
out={}
for s in db.execute(select(PricingSku)).scalars().all():
    p=promo.get(s.sku_code)
    if not p: continue
    rp=pricing_calc_service.report_prices(p,params)
    out[s.sku_code]={"rp":float(rp["report_price"]) if rp.get("report_price") is not None else None}
# dump json → cat 回本地
```
报名价字段：88VIP大促=`report_price`；超大促618=`report_price_618`（见 build_promo_signup 的 `_PROMO_SIGNUP_TIERS`）。

---

## 6. 坑 / 教训（别再踩）
- **系统内SKU名 ≠ 淘宝文案**（`基础柜-下柜-1.2米` vs `储物下柜1.2米`），按字面相似度自动匹配会串（曾把榉木床改松木码、6把不同椅子贴成1个码）。**匹配靠尺寸(跨语言中性)校验，绝不盲推**。
- `commit_sheet` 返回的action是主表(product_code)动作，**掩盖promo子表taobao_sku_id的覆盖**（报"317skipped/17updated"骗人）→ 用独立 verify_delta 查真实delta。
- 商品导出M列(价格)有的是被虚高的一口价/标价，`报名价÷M` 会显得折扣很深（孚格0.16那种），**多半是那个产品定价配错(如603)，不是折扣本身**。
- 淘宝导出xlsx有的用**流式zip(数据描述符)**，openpyxl `read_only=True` 会报"not a zip"，改 `read_only=False` 能读。
- 控制台GBK打印中文/emoji会崩(UnicodeEncodeError)——写UTF-8文件再Read，别指望print。
- GateGuard钩子：每次Edit/Write/首个Bash要先摆4条facts。

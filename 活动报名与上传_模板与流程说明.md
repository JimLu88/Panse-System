# 畔色 · 活动报名与上传（模板 · 口径 · 流程）权威说明

> 放在仓库根目录，作为**淘宝活动报名/上传**这块的唯一权威说明，省得每次重新摸。
> 与定价口径配套文档：根目录 `价格体系设置.md`（报名价怎么算）。最后更新 2026-07-13。

---

## 〇、一句话全景

我们有 **3 个互不相同的淘宝营销渠道**，每个渠道**有自己的表模板、自己的入口、自己的上传方式**，绝不能混：

| # | 渠道 | 系统 channel | 模板（assets/taobao_templates/） | 列数 | 千牛入口 |
|---|---|---|---|---|---|
| A | **单品立减** | `single_item_discount` | `single_item_discount.xlsx` | 减金额表 | 营销工具 → 单品立减 → create 页 |
| B | **大促活动报名** | `promo_signup` | `promo_signup_sku.xlsx` | 7 列 | 营销活动 → 活动报名 →（如「7月超级88」）→ 商品批量导入 |
| C | **超级立减长期活动** | `super_reduce` | `super_reduce_import.xlsx` | 14 列 | 营销 → 超级立减 → 报名页 `apply.htm` → 商品批量导入 |

**B 和 C 是两个不同的活动、两套不同模板**（用户 2026-07-13 明确：分成两个按钮，分别导出，不要合成一个）：
- **B 大促活动报名** = 有档期的平台大促（超级88、618、双11 等），7 列模板只填 `商品ID / SKUID / 活动价`。
- **C 超级立减长期活动** = 常年在线的超级立减（2025-05-31 起延到 2028-07-31），14 列模板填 `商品ID / SKUID / 活动价 / 让利比例 / 补贴金额` 等。

前端位置：**定价页 → 「🚀 活动自动填写」tab**。Step2 = 单品立减(A)，Step3 = 大促活动报名(B)，Step3 下方独立卡片 = 超级立减长期活动(C)。

---

## ★★★ 第一铁律：以 ERP 价格为准，不迁就平台（2026-07-13 用户拍板，最高优先级）

**ERP 的价格就是我们所有价格的唯一标准。报名一律按 ERP 价格报，绝不为了迁就平台规则去改 ERP。平台报不进（标价超线等），是【千牛配置】填错了 → 去改千牛对齐 ERP，绝不反过来改 ERP。**

- **报名价 = ERP 日常价**（`sku.daily_price`）。超级立减、单品立减都以它为基准。
- **千牛 SKU 一口价（标价）必须 = ERP 日常价 ÷ 0.75**（0.75=单品宝标准折，永不改）。→ 一口价 × 0.75 = ERP 日常价。
- 若报名被平台拒（"活动价/标价不得高于近15天最低标价"），根因几乎都是**千牛一口价填低了**（如蜂蜜餐桌千牛3440、×0.75=2580 < ERP日常价2827.5）→ **改千牛一口价到 ERP日常价÷0.75=3770**，别动 ERP。
- 详见 `价格体系设置.md` 开头「★★★第一铁律」+ 记忆 `feedback_erp-price-is-standard`。

### ★ 报名失败排查四个价格点（按此顺序查）
一个 SKU 报不进，问题一定在这 4 处之一：

| # | 价格点 | 正确值 | 常见错法 |
|---|---|---|---|
| 1 | **标价（一口价）** | = ERP日常价 ÷ 0.75 | 填低了→单品宝价<ERP日常价→报名价超线被拒（改千牛一口价） |
| 2 | **单品宝（新品促销 0.75）** | 挂着、75折 | 没挂→标价=一口价没打折（去千牛挂单品宝） |
| 3 | **单品立减（减金额）** | = 日常价×0.9 − 目标到手 | 按封顶价算错/旧值/冲突活动压着（见 §八；活动价变了要配套重算） |
| 4 | **报名价（活动价）** | 超级立减=日常价；大促=报名价A | 超级立减误填报名价A→双折砸穿（§一 ★★）；活动价只能下调不能上调，抬价要先撤销 |

- **上调锁**：已报名的活动价"只能下调不能上调"。要把活动价往上抬（如从低价改回ERP日常价），**必须先在千牛撤销该活动报名**，再重报。
- 无单品宝的SKU（如样块，标价=一口价）：一口价直接=ERP日常价。

---

## 一、只有两个报名价（★最容易搞混，先记这个）

详见 `价格体系设置.md`。核心：

- **报名价 A** = 大促到手 ÷ (1 − 12%) = 中促到手 ÷ (1 − 10%)（两式相等 = 合规）。
- **报名价 618** = 大促到手 ÷ (1 − 15%)（只有 618/双11 用，且要换 SKU）。
- 到手价永远比店铺实收价高一点（88VIP 佣金 2%）：**弹窗"目标到手" × 0.98 = 定价表「大促价」列**。
- 改毛利率**只动大促基数**（`base_big`），绝不手改价：`毛利率 M = 0.974 × (1 − base_big)`，如 `base_big=0.846 ⇔ M=15.0%`。

三个渠道用哪个价：

| 渠道 | 填什么 | 用哪个价 | 买家到手 |
|---|---|---|---|
| A 单品立减 | 减金额 = 日常价×(1−官方力度) − 目标到手 | 到手锚在中/大促 | 日常价 − 官方立减 − 单品立减 |
| B 大促报名 | 活动价 = **报名价 A**（618 档用报名价 618） | A / A618 | A × (1−场次力度) |
| C 超级立减长期 | **活动价 = 日常价**；让利10；补贴留空 | **日常价** | 日常价 层层叠(单品立减+88VIP) → 实付≈大促到手，毛利~22% |

> ★★C 超级立减长期活动的活动价是**日常价**，**绝不是报名价A**（2026-07-13 血泪根治，1105单实测）。它走**单品立减法**：活动价填日常价，折扣由并行的单品立减+88VIP叠加。填报名价A会双重打折砸穿成本（造成过37商品低价上线亏损单）。详见 `价格体系设置.md` §六.②b。

---

## 二、C 超级立减长期活动（14 列模板）—— 重点说清

### 2.1 模板结构（平台原始模版，严格）
`super_reduce_import.xlsx` 有 **两个 sheet**：
- `模版说明`（平台填表须知，原样保留）
- `商品SKU导入列表`（**前 3 行是表头**：组名行 / 列名行 / 填写说明行；**数据从第 4 行起**）

**14 列**（列序固定，不可改）：
`商品ID | SKUID | 活动价 | 库存 | 包邮 | 商品短标题 | 商品场景图1:1 | 通用商品白底图 | 短视频×4 | 让利比例 | 补贴金额`

平台明确警告"**请勿对本 Excel 做任何改动，否则重新下载模板**" → 系统生成时**加载真实模版、只往第 4 行起写数据**，保留两个 sheet + 3 行表头。代码：`data_export_service.build_super_reduce_signup_upload_xlsx`（2026-07-13 改为填真实模版，不再另生成裸表）。

### 2.2 每列怎么填
| 列 | 值 | 必填 |
|---|---|---|
| 商品ID | 淘宝商品ID（taobao_item_id） | 必填 |
| SKUID | 淘宝 SKUID（主 + alt 各一行） | SKU维度必填 |
| 活动价 | **报名价 A**（占位SKU=占位报名价） | 必填 |
| 包邮 | "包邮" | 现况全包邮 |
| 让利比例 | 10（=10%，一位小数） | ★只填这个 |
| 补贴金额 | **留空** | ★留空 |
| 库存/短标题/素材 | 留空（库存空=全部库存） | 非必填 |

到手 = 活动价 × (1 − 10%) = 活动价 × 0.9 = 中促到手。

**★平台铁律（2026-07-13 实测 60/60 失败反馈锤实）**：`让利比例` 和 `补贴金额` **只能填一个**！两列都填 → 平台报"玩法数据错误：让利比例,补贴金额 只能填写一个" → 整商品拒。**系统只填让利比例、补贴金额必须留空**（千牛逐商品编辑页原生也用让利比例）。修好后重测 60→37 成功。

### 2.3 千牛在哪操作（★之前踩坑：探错页）
- **招商页 `ttmjpt.htm`**（营销 → 超级立减）**没有批量导入**，只有逐商品「编辑商品」。← 别在这找。
- **报名页 `apply.htm?signRecordId=<报名记录ID>&puCode=itemApply`**（从活动报名进具体活动）**才有**「商品批量导入」：第一步下载模版 / 第二步导入表格。← 在这导。
- 导入后进**草稿 / 已发布设定**，需**「一键发布」**才在活动开始时生效。tabs：草稿/待审核/审核通过/撤销报名/**活动中**/…
- 平台限制：模版说明"每次导入最多 500 商品"，玩法说明"最多 1000 条记录"。我们 60 商品 / 526 SKU，都在限内。
- 已在**活动中**的商品价格：批量导入通常创建新草稿版本；也可逐商品「编辑商品」直接改活动价（系统的 super_reduce 自动上传走这条，见 §四）。

### 2.4 手动上传（最稳）
1. 定价页 → 活动自动填写 → **超级立减长期活动 报名表**（下载，已填好真实模版，可直接导入）；
2. 千牛报名页 → 商品批量导入 → **第二步 导入表格** → 传这个文件；
3. → 草稿 → **一键发布**。

---

## 三、B 大促活动报名（7 列模板）—— 不变

- 模板 `promo_signup_sku.xlsx`，sheet `商品SKU导入列表`，前 3 行表头，数据从第 4 行；**只填 商品ID / SKUID / 活动价(报名价)**，其余留空（库存/发货/官方立减折扣/金额）。
- 3 档：88VIP大促 12%（big，报名价 A）/ 中促 10%（mid，报名价 A）/ 超级大促 15%（big618，报名价 618，换 SKU）。
- 千牛：活动报名 → 进具体活动（如 7月超级88）→ 商品管理 → 商品批量导入 → 导入表格 → 落草稿 → 手动「发布报名」。
- 代码：`data_export_service.build_promo_signup_upload_xlsx(db, tier)`。

---

## 四、系统自动上传（stage 预演 → 比对表 → commit 提交）

前端「上传到千牛（先比对）」按钮走 Web-Agent（PC :8500）自动化：
- **stage**：挂文件/填价，**停在提交前**，回**比对表**（上传值 vs 系统应填值，0 容差核对）+ 千牛校验 + 截图。
- **commit**：★不可逆★ 真提交，只在用户看过比对表点确认后。

各渠道 commit 现状：
| 渠道 | stage | commit |
|---|---|---|
| A 单品立减 | ✅ 填表单停在确认设置前 | ✅ 点「确认设置」 |
| B 大促报名 | ✅ 挂到千牛草稿 + 比对 | ⏳ 最终「发布报名」暂手动 |
| C 超级立减 | ✅ **报名页 apply.htm 批量导入到草稿**（可撤销）+ 出导入结果 | ✅ **批量导入 + 一键发布**（异步，前端轮询进度） |

代码：Web-Agent `app/engine/uploader.py`（`stage_/commit_super_reduce` → `_run_super_import(publish=)`，导航到 `apply.htm` → 商品批量导入 → 导入表格 file_chooser → 轮询结果 → publish 时点一键发布）+ ERP `activity_upload_service.stage/commit/commit_status`。
- apply.htm 的 `signRecordId`（长期活动的报名记录ID）稳定，写死在 `_SUPER_APPLY_URL_DEFAULT`；换活动可在 WA `settings.json` 设 `super_reduce_apply_url` 覆盖。
- **实测（2026-07-13）**：60 商品导入 → **37 成功 / 23 失败**。失败全是业务原因（非自动化 bug）：动销不达标(0销量)14 / 活动价·最低标价·红线风险~16 / 整商品缺SKU(无映射)3 / 标价超15天线2 / 已下架SKU 1。失败原因自动抓回（`点此下载操作反馈`→`失败原因或风险提示`列）显示在弹窗。

### 4.1 登录扫码自愈（2026-07-13 根治）
- 千牛登录页默认**密码登录**，可扫大二维码藏在右上角"扫码登录更便捷"切换后 → 老代码截到的是没用的小图标。
- **根治**：登录墙一出现，自动导航到淘宝**统一登录页** `login.taobao.com/havanaone/login/login.htm?bizName=taobao`，那里**默认就渲染 240px 可扫大码**，无需点任何切换；统一 SSO，扫一次全站通。截 `.qrcode-login` 元素推飞书。代码 `uploader._reveal_and_push_qr` + `_ensure_login`。
- 用户要**自己在屏幕上登录**：WA 平时把窗口停在 `-32000` off-screen（看不见）；需要可见时用 `--window-position=80,40` 开持久 profile 窗口（脚本 `visible_login.py`）。

---

## 五、上传前必做：预检（虚拟推送）

定价页 → 活动自动填写 → **「开始虚拟推送」**（不产文件、不改数据、不上传）。检查：
- **坏价产品**（自动从表排除，改真实价后自动纳入）；
- **淘宝 SKUID 撞号**（一个 SKUID 绑多个商家编码 → 上传两行打架必串价，先改映射）；
- **券后价超线 / 整商品不完整**（淘宝必拒，纳入"全绿"判定）；
- **近 60 天 0 销量**（橙色警示，不拦，但报名前主动提醒——平台动销规则可能拒）；
- **15 天最低价冲突**（初始报价可勾选跳过；只是预警不拦上传）。

全绿再生成/上传。

---

## 六、铁律（血泪坑）

1. **B 和 C 是两个活动、两套模板，分开导出，绝不合一。**
2. **填模板必须用平台真实模版**（加载 asset，数据从第 4 行起），保留两个 sheet + 3 行表头，别另生成裸表——否则导入被拒/错列。
2b. **超级立减：让利比例 与 补贴金额 只能填一个**（只填让利比例10，补贴留空）；两列都填必被拒。
3. **商家编码跟尺寸走，不跟槽位走**（SKUID 轮换见 `价格体系设置.md` §九）；SKU 属性编辑（加"免安装"等）会**换 SKUID**，商品价格导出的 SKU 级商家编码列是权威 join 键。
4. **超级立减找批量导入去报名页 `apply.htm`，不是招商页 `ttmjpt.htm`。**
5. **改毛利只动大促基数**，绝不手改价；改价自动版本化（老单老价）。
6. 三方部署铁律：NAS 镜像 + PC 源码 + GitHub 同一 commit；PC 热补后必 `docker commit` 固化镜像。

---

## 七、关键代码/文件位置

- 生成器：`backend/app/services/data_export_service.py`
  - `build_single_item_discount_upload_xlsx` / `build_promo_signup_upload_xlsx` / `build_super_reduce_signup_upload_xlsx`
  - `collect_signup_rows`（B/C 共享收集器：占位封顶到已生效价 + 整商品完整性剔除）
- 上传编排：`backend/app/services/activity_upload_service.py`（stage/commit/commit_status）
- 预检：`backend/app/services/activity_preflight_service.py`
- 定价引擎：`backend/app/services/pricing_calc_service.py`（report_prices/single_item_discounts/recompute）
- 模板资产：`backend/app/assets/taobao_templates/`（`promo_signup_sku.xlsx` / `super_reduce_import.xlsx` / `single_item_discount.xlsx`）
- Web-Agent：`D:\Panse-Web-Agent\app\engine\uploader.py`（本地无 remote，只 PC 跑）
- 前端：`frontend/src/pages/ActivityAutoFillTab.tsx`（活动自动填写 tab）
- 配套口径：根目录 `价格体系设置.md`

---

## 八、单品立减「活动冲突 / 衔接」（★2026-07-13 实测锤死）

**同一商品同一时间只能被一个"生效中"的单品立减命中。** 一旦已有一个 **进行中的商品级单品立减**（如同事为 88 大促设的「88XX」打折活动，档期 07-07~07-19），你再导入一个 **SKU 级单品立减**：

- **导入会显示成功**（"导入成功 N 条失败 0 条"、活动列表里新活动"进行中"），**但券后价不变**——因为进行中的旧活动在压着，新活动排不上。
- 这不是代码 bug，也不是导入失败，纯粹是**活动冲突/优先级**。
- **正解**：先把冲突的那个**进行中活动「暂停/撤出」**，新 SKU 级立减才生效。（2026-07-13：840643621692 就是暂停了进行中商品级活动后，SKU 级立减 3036 券后价才落地。）

**★验证看哪里（别再看错）**：单品立减页 → **「活动列表」tab**（看到自己刚导的 SKU 级活动 + 右侧「导入历史」成功/失败），**不是「商品列表」tab**（那个按商品聚合、会把商品级冲突活动混进来，看不清自己导的）。

### 8.1 建议（待自动化 · 后续我来做）

**活动衔接自动暂停**：系统在上传新一档单品立减前，自动探测该批商品**当前进行中的旧单品立减活动**并**先暂停它**，再上传新活动 → 实现"上一档→下一档"无缝衔接，免去人工先去千牛暂停。当前（测试阶段）暂停仍是**人工**：由人先在千牛把冲突活动暂停，我再上传。

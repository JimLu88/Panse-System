# 发货报表「密码解密」对接方案(交给 Panse-System 会话实现)

> 来源:Panse-Web-Agent 会话调研产出。Web-Agent 已把"下载加密发货报表 + 点发送密码"做完;
> 缺的是 **Panse-System 侧**:① 从飞书机器人抓到密码并存下;② 导入发货报表时用密码解密。
> 用户已拍板走「转发到飞书机器人自动抓」这条路。**先看完本文件再动手。**
>
> **现行规则（用户 2026-07-27 明确确认）**：发货报表口令没有时间限制。系统保存最近口令，
> 不得按 60 分钟、24 小时或其他年龄判过期。收到口令后应自动完成
> `解密 → 入库 → 新鲜度收口 → 生成下单图 → 飞书增量推送`；口令不匹配时保留待口令状态并明确报告，
> 不得误报“口令过期”。

---

## 1. 背景与数据流

淘宝「发货报表」含收货人手机号(隐私),导出文件是**加密**的。下载流程:
1. 在已生成报表列表点「下载发货报表」→ 得到一个**加密文件**(口令保护)。
2. 点「发送密码」→ 淘宝把**该次导出的口令**经**微信强提醒**发到用户手机。
3. 用户把这个口令**转发到飞书群/机器人**。
4. **(待实现)** Panse 飞书 webhook 抓到口令存下 → 导入发货报表时解密。

> 口令是**一次一密**(每次点发送密码生成新口令),不是账号级固定密码。

Web-Agent 侧已完成(无需 Panse 改):
- 收取工作流 `data/workflows/taobao__shipping__collect.json`:goto 列表页 → `download`「下载发货报表」→ `click`「发送密码」。
- 加密文件落在 `D:\AI\Panse-Web-Agent\data\output\<YYYY-MM-DD>\taobao\ExportOrderList<id>.xlsx`。

---

## 2. 要在 Panse-System 实现的两块

### A. 飞书机器人抓密码并存下

**挂载点**:`backend/app/services/feishu_webhook_service.py` → `_maybe_bot()` 已把
`im.message.receive_v1` 分发给 `feishu_bot_service.on_message_event(db, event)`。
在 `feishu_bot_service.on_message_event` 里(或其调用链)加一个**密码识别分支**。

**识别约定(二选一,建议用前者,防误判)**:
- **命令前缀**:用户转发时带一句,如 `发货密码 Ab3xZ9`(或 `发货密码:Ab3xZ9`)。
  机器人正则 `^发货密码[:： ]\s*(\S+)$` 取捕获组。
- **纯口令裸消息**:风险高(任何短消息都可能被当口令),不建议。

**存储**(任选,建议系统设置即可,无需新表):
- `settings_service.set(db, "taobao_shipping_pwd_latest", <pwd>)`
- `settings_service.set(db, "taobao_shipping_pwd_at", <ISO8601 当前时间>)`

> 一次一密 + 用户串行操作 → "最新口令 + 时间戳"足够；时间戳只用于审计，不作失效判断。

机器人收到后**回一条确认卡片/文字**，并报告自动解密、入库与下单图续推的实际结果。

### B. 导入发货报表时解密

**挂载点**:`backend/app/services/taobao_order_import.py`(它已能解析千牛多表里的
`发货报表` Sheet,但假设是**明文**)。在**读取文件之前**加一步"按需解密":

```python
def _maybe_decrypt(path: str, password: str | None) -> str:
    """加密的 xlsx/zip → 解成明文临时文件并返回新路径;明文则原样返回。"""
    with open(path, "rb") as f:
        head = f.read(8)
    # OOXML 加密(Office 复合文档)magic = D0 CF 11 E0 A1 B1 1A E1
    if head[:8] == b"\xd0\xcf\x11\xe0\xa1\xb1\x1a\xe1":
        import msoffcrypto, tempfile
        if not password:
            raise ValueError("发货报表已加密,但未取到飞书口令")
        out = tempfile.NamedTemporaryFile(delete=False, suffix=".xlsx").name
        with open(path, "rb") as fin, open(out, "wb") as fout:
            office = msoffcrypto.OfficeFile(fin)
            office.load_key(password=password)
            office.decrypt(fout)
        return out
    # 口令保护的 zip(magic = 'PK')→ 内含 xlsx
    if head[:2] == b"PK":
        import pyzipper, tempfile, os
        with pyzipper.AESZipFile(path) as z:
            if password:
                z.setpassword(password.encode())
            inner = next(n for n in z.namelist() if n.lower().endswith((".xlsx", ".xls", ".csv")))
            data = z.read(inner)
        out = os.path.join(tempfile.gettempdir(), os.path.basename(inner))
        with open(out, "wb") as fo:
            fo.write(data)
        return out
    return path  # 明文,直接用
```

调用处:`path = _maybe_decrypt(path, settings_service.get(db, "taobao_shipping_pwd_latest"))`
然后用 `path` 走现有解析逻辑。

**依赖**:`pip install msoffcrypto-tool pyzipper`(加进 `backend/requirements.txt`)。
> 实际格式(加密 xlsx vs 口令 zip)**需用一份真实下载的发货报表确认**——两条分支都给了,
> 跑通后可删掉用不到的那条。下载一份真文件:在 Web-Agent 跑一次发货报表收取即可拿到样本。

### C. 口令↔文件 配对

- 一次一密 + 用户串行 → **取最新口令**即可；不做时间窗失效判断。若口令与报表不匹配，
  解密会自然失败，文件保持待口令状态并提示提供对应口令。
- 不必精确配对到具体文件 ID(用户一次只导一份发货报表)。

---

## 3. 待确认/决策点(Panse 会话动手前定一下)

1. **真实文件格式**:✅ **已实测确认 = 加密 OOXML 文档**(前8字节 `D0 CF 11 E0 A1 B1 1A E1`,约 29KB)。
   走 `msoffcrypto-tool` 那条分支即可,**口令 zip(PK)分支用不到,可删**。(2026-06-11 用一份真实发货报表验证)
2. **密码识别约定**:确定用 `发货密码 <口令>` 这类命令前缀(推荐),并在机器人回执里写明格式。
3. **存储位置**:`system_settings` 两个键够用,还是要建专表留历史?(建议先用设置项)。
4. **文件如何到 ERP**:当前加密文件在 Web-Agent 的 output 目录。两系统未来合并前,
   先约定一个**共享落盘目录**或导入时指定路径。

---

## 4. 与 Web-Agent 的边界(明确职责)

| 职责 | 归属 | 状态 |
|---|---|---|
| 登录淘宝、下载加密发货报表、点发送密码 | Web-Agent | ✅ 已完成 |
| 飞书 webhook 抓口令并存下 | **Panse-System** | ⬜ 本方案 A |
| 发货报表导入时解密 | **Panse-System** | ⬜ 本方案 B |
| 把加密文件交付到 ERP 可读位置 | 两边约定 | ⬜ 决策点 4 |

> 备注:Web-Agent 未来会并入 Panse-System,届时 output 目录与导入流水线打通,
> "交付位置"问题自然消失;在此之前用共享目录过渡即可。

---

## 5. 需要人工扫码时 → 强提醒通知用户(新增)

### 场景
某些步骤是平台**反欺诈刻意设计**的,必须本人当场操作,自动化做不到无人值守:
- **支付宝流水导出**:点「下载」会弹「手机打开支付宝·扫描二维码验证身份」(每次会话约一次)。
  必须本人用支付宝 App 扫码,cookie 替不了(验证态短期有效、跨会话失效)。
- (发货报表口令转发 = 见第 1~2 节,也是人工环节。)

### 需求
自动化跑到这种"卡点"时,不要让浏览器干等到超时,而是**主动推一条强提醒**给用户:
"支付宝流水导出需要你扫码验证,请到运行机扫一下二维码"。用户扫完,流程继续。

> 现实说明:本系统**发不出淘宝/支付宝那种原生"微信强提醒"**(那是平台自己发的)。
> 我们能做的是经 **Panse 已有的机器人通道**推一条**加急/强提醒**消息,效果等价。

### 实现分工
- **Web-Agent(检测端)**:运行中检测"需要扫码"状态 → 暂停、保持浏览器打开 → 调通知钩子。
  - 识别特征:页面/弹窗含文案「扫描二维码验证身份」「验证身份」,或出现二维码元素(`img`/`canvas` + 上述文案)。
  - 钩子:`notify(level="urgent", text="支付宝流水导出需扫码验证,请到运行机扫码")`。
- **Panse-System(发送端)**:提供"发强提醒"函数,Web-Agent(或合并后)调用。
  - **飞书**:用 tenant_access_token 调 `POST im/v1/messages`,msg_type=`text`;飞书的「**加急/buzz**」(`urgent_app`/`urgent_phone`)≈ 强提醒,可叫醒手机。
  - **企业微信/服务号**(若要更接近"微信强提醒"):用模板消息/应用消息通道,按现有配置接。
  - 接收人:用户 open_id 或固定群。

### 交互与兜底
- 通知发出 → 用户到运行机扫码 → 浏览器继续 → 完成下载。
- 超时兜底(如 5 分钟没扫)→ 本次跳过、标记「待人工」,下次再跑。

### 进阶:把二维码图片发到飞书,让用户远程扫码(用户明确要求)

**痛点**:支付宝流水导出点「下载」会弹「手机打开支付宝·扫描二维码验证身份」。如果只发文字提醒,
用户还得跑到运行机前才能扫。**更好:直接把那张二维码图片发到飞书,用户在手机上对着飞书里的图就能扫。**

**做法**:
- **Web-Agent(检测端)**:检测到验证弹窗后,**截取二维码元素的图片**(`locator(二维码img/canvas).screenshot()` 得到 PNG 字节),连同提示文字一起交给通知钩子:
  `notify(level="urgent", text="支付宝流水导出需扫码验证,扫这张图👇", image=<png_bytes>)`。
- **Panse-System(发送端)**:飞书发图片消息——先 `POST im/v1/images`(`image_type=message`)上传得 `image_key`,
  再 `POST im/v1/messages`(`msg_type=image`,`content={"image_key":...}`)发出;随后再发一条 `urgent_app/urgent_phone` 加急叫醒手机。
- **时效**:支付宝二维码有效期短(~1-2 分钟)且**会话本身很短命**,所以:① 检测到就**立刻**发,不要等;② 工作流要能**等待用户扫完**(轮询页面是否离开验证态,最多等 ~2 分钟)再继续;超时则本次放弃、标「待人工」。

> 注:这条同样适用于任何"运行中途要扫码"的平台,不限支付宝。

### 职责补充(并入第 4 节表)
| 职责 | 归属 | 状态 |
|---|---|---|
| 检测"需要扫码/人工"状态并暂停 | Web-Agent | ⬜ 本节 |
| 截取二维码图片 + 调通知钩子(带图) | Web-Agent | ⬜ 本节 |
| 提供"发图片+加急"发送函数(飞书 im/v1/images+messages) | **Panse-System** | ⬜ 本节 |

> 目标不是消灭人工(平台反欺诈消灭不了),而是**把人工降到最小 + 该你出手时第一时间喊你**。

---

## 6. 支付宝余额/流水改走官方 API(强烈推荐,替代截图)

### 为什么换
当前用浏览器截图读支付宝余额,有三个根治不了的痛点:**①商家平台 b.alipay.com 会话十几分钟就过期 ②流水导出要扫码验证 ③有风控**。
支付宝开放平台有官方接口,**直接返回数字、长期密钥、无风控、无会话过期、秒级**。这才是正路。

### 可用接口(支付宝开放平台,已核对 2026-06-12)
- **余额** `alipay.data.bill.balance.query`(商家账户当前余额):自查自己账户 **biz_content 留空 `{}` 即可**,响应含 `total_amount` / `available_amount` / `freeze_amount`。文档:https://opendocs.alipay.com/open/2acb3c34_alipay.data.bill.balance.query
  - 历史余额:`alipay.data.bill.balancehis.query`
- **流水/对账单** `alipay.data.dataservice.bill.downloadurl.query`(查对账单下载地址):传 `bill_type`(`trade`=交易账单 / `signcustomer`=资金账单)+ `bill_date`(`YYYY-MM-DD` 或 `YYYY-MM`),返回 `bill_download_url`。**约束:链接约 30 秒失效(拿到即下);只能下上月及更早;次日 10 点后可查**。文档:https://opendocs.alipay.com/open/e099d91f_alipay.data.dataservice.bill.downloadurl.query
> 两者都需在应用「可调用产品」里**签约对应产品**,否则报「ISV权限不足」。
> Web-Agent 侧已实现 `app/engine/alipay_api.py`(RSA2 签名 + `query_balance`/`query_bill_download_url`/`download_bill`)+ 设置页凭据录入(私钥 keyring 加密)+ `/api/alipay/test` 自测端点,待用户配好密钥即可联调。

### 用户先试:开通步骤(一次性)
1. **登录** https://open.alipay.com/,用**支付宝商家账号**。
2. **创建自用应用**:控制台 → 创建应用 → 选「网页&移动应用」(自用型)→ 记下 **APPID**。
3. **配密钥**:用支付宝「密钥生成工具」生成 **RSA2 密钥对**;**应用公钥**上传到应用,留好**应用私钥**;复制平台给的**支付宝公钥**。(文档:https://opendocs.alipay.com/common/02kipl)
4. **加能力/签约**:在应用里添加资金类接口能力(如「资金账户资产查询」),部分需签约或审核通过。
5. **授权(自查自己账户)**:走「应用授权」让商家把应用授权给自己,拿 `app_auth_token`;部分接口用应用级身份即可。
6. **验证**:用支付宝「接口测试工具」或 SDK 调一次 `alipay.fund.account.query`,能返回可用余额即通。

### 落地到 Web-Agent / Panse(写进对接,供实现)
- 新增「API 取数」层(Web-Agent 已有 Tier-0 `http_export` 的位置很合适):
  - 凭据:`app_id` / `应用私钥` / `支付宝公钥` / `app_auth_token`,**存 keyring(加密),私钥永不入库明文**。
  - SDK:Python 用官方 `alipay-sdk-python` 或社区 `python-alipay-sdk`,RSA2 签名调用。
  - 任务运行时**优先走 API**(无浏览器、无风控、拿到数字直接喂 ERP),API 不可用再回退当前截图方案。
- 收益:支付宝余额/流水从"脆弱截图+频繁扫码"升级为"稳定 API 取数",彻底绕开会话过期与风控。

### 职责
| 职责 | 归属 | 状态 |
|---|---|---|
| 开放平台建自用应用、配密钥、签约、授权 | **用户**(一次性) | ⬜ 你先试 |
| API 取数层 + 凭据加密存储 + 优先级编排 | Web-Agent / Panse | ⬜ 待开通后实现 |

> 同理,**淘宝/万相台**也有开放平台 API(淘宝开放平台 / 阿里妈妈),商家自查余额的开放度参差,可同法评估;能走 API 的都优先走 API。
